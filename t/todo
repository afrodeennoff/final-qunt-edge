# Backend Performance & Reliability Rework Program

**Created:** 2026-04-16
**Branch:** codex-obsidian-v3-redesign
**Scope:** Backend-only, no UI/route-shape changes in Phase 1

## Current UI Audit

### Admin + Teams + Auth UI unification (2026-04-19)
- [x] Normalize admin surfaces: `border-[oklch(0.65_0.22_260/0.08)]` → `border-border/45`, `bg-background/72` → consistent surfaces
- [x] Normalize teams surfaces: fix `bg-white/[0.5]` hover in navbar, align layout with admin shell
- [x] Normalize auth surfaces: fix radial gradients for obsidian consistency, refine entry chrome
- [x] Remove `text-red-500` hardcoded → `text-destructive` or oklch tokens
- [x] Standardize form group surfaces across admin pages
- [x] Improve admin page Suspense fallback to use shared route-state language
- [x] Verify with `npx tsc --noEmit`

### Trader profile metric-band rebalance (2026-04-18)
- [x] Move the visibility and review-window controls from the detached hero rail into the summary metric board
- [x] Rebuild the seven quick metrics into a two-row `3 + 4` hierarchy while keeping the existing trader-profile logic untouched
- [x] Increase the calendar panel height, add smoother staged page-entry motion, and re-verify with targeted lint + typecheck

### Admin coupons + prop-firms repair (2026-04-18)
- [x] Add failing tests for coupon scheduling visibility and admin datetime-local formatting, then implement the shared helpers they need
- [x] Refactor `/admin/propfirms` list/detail routes to use a cleaner admin layout and fully disable write actions in fallback/read-only states
- [x] Re-verify the repaired admin surfaces with targeted tests, lint/typecheck, then persist memory/lessons/pattern updates

### Admin coupons + prop-firms visual refinement (2026-04-18)
- [x] Rebuild `/admin/coupons` into a cleaner single-column stacked workspace with calmer section hierarchy
- [x] Polish `/admin/propfirms` list/detail layouts to match the refined coupon workspace and reduce visual clutter
- [x] Re-run lint/typecheck, review the diff, and persist the refined admin UI patterns

### Full-site unified product-led redesign (2026-04-18)
- [x] Decompose the full-site redesign into shared foundations plus Wave 1 public, Wave 2 dashboard/teams, and Wave 3 admin/internal surfaces
- [x] Lock the visual direction with the approved constraints: product-led, unified across all pages, visual-only, no gradients, and hybrid product-UI plus restrained animated SVG media
- [x] Write the design spec in `docs/superpowers/specs/2026-04-18-full-site-unified-product-led-redesign-design.md`
- [x] Write the implementation plan in `docs/superpowers/plans/2026-04-18-full-site-unified-product-led-redesign-implementation-plan.md`
- [x] Start execution only after explicit approval on the implementation plan
- [x] Refresh the shared foundation plus the main home/public shell and hero system
- [x] Refresh the live dashboard header, the mounted teams workspace shell/pages, and the mounted admin shell/dashboard around the same shared product-led system
- [ ] Continue the rollout from the new shared foundation: finish long-tail public cleanup, then tighten remaining dashboard/teams/admin route-specific pages on top of the refreshed shared shell
- [ ] Ship the redesign in tracked waves with lint, typecheck, responsive review, and project-note updates after each wave

### Global page-shell + motion unification (2026-04-17)
- [x] Audit the remaining live landing, dashboard, admin, and teams routes that still bypass the `/deals` composition language or shared shell rhythm
- [x] Extract or standardize the shared section/motion recipes needed to carry the `/deals` design system across those routes without reintroducing one-off page chrome
- [x] Apply the unified shell, surface hierarchy, and stronger-but-restrained animation language to the remaining route groups in focused waves
- [x] Re-verify with targeted lint + typecheck, then persist the durable design-system and animation rules to the project notes

### Trader profile diagram alignment (2026-04-17)
- [x] Reshape `app/[locale]/dashboard/trader-profile/page-client.tsx` to match the rough wireframe more closely: profile + visibility row, thin signal strip, active-accounts block, calendar block, right-side benchmark/portfolio/risk stack, and full-width trade history block
- [x] Keep the existing trader-profile calculations and interactions intact while tightening the section order, card proportions, and horizontal balance to mirror the diagram
- [x] Re-verify the updated trader-profile page with targeted lint + typecheck and persist any durable layout guidance to the project notes

### Trader profile + deals experience enhancement (2026-04-17)
- [x] Audit `app/[locale]/dashboard/trader-profile/page-client.tsx` and `app/[locale]/(landing)/deals/components/deals-experience.tsx` for weak hierarchy, uneven section balance, and inconsistent surface styling
- [x] Refine trader profile into a cleaner overview/performance/story layout with stronger stat grouping, better aside structure, and more intentional spacing across breakpoints
- [x] Rebuild the deals page shell, hero, filter board, spotlight carousel, and deal cards onto a more cohesive Electric Obsidian layout with clearer sections and stronger CTA hierarchy
- [x] Verify with targeted lint + typecheck, then persist the durable UI/design notes into the project markdown files

### Dashboard spacing + trader profile refinement (2026-04-17)
- [x] Audit `app/[locale]/dashboard/*` route shells for inconsistent padding, max-widths, and vertical rhythm
- [x] Normalize dashboard/trader-profile responsiveness so sections balance horizontally on desktop and stack cleanly on smaller widths
- [x] Refine `app/[locale]/dashboard/trader-profile/page-client.tsx` to use shared shell patterns, cleaner information hierarchy, and more resilient card/grid spacing
- [x] Verify with targeted type/lint checks and a visual pass where feasible

### Admin coupon fallback sync (2026-04-17)
- [x] Trace why public deal cards can show discount/code/claim values while admin coupon screens still appear empty
- [x] Add shared spotlight-to-coupon suggestion logic so admin can save fallback public deals as real editable coupons
- [x] Surface prefilled coupon suggestions in both `/admin/coupons` and `/admin/propfirms/[id]`
- [x] Verify with targeted lint and full typecheck

### Dashboard route surfaces UI polish (2026-04-19)
- [ ] Normalize dashboard-header.tsx: replace hardcoded rgba/white tints with border-border/foreground tokens
- [ ] Remove dead navbar.tsx (no imports, replaced by dashboard-header.tsx)
- [ ] Fix billing/page.tsx double-frame (Card inside UnifiedSurface)
- [ ] Refine settings/page.tsx: normalize surfaces, fix variant="error"→"destructive", improve hierarchy
- [ ] Wrap import callback pages in UnifiedPageShell for visual consistency
- [ ] Verify with npx tsc --noEmit
