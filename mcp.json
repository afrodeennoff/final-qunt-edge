{
  "mcpServers": {
    "qunt-edge": {
      "type": "mcp",
      "url": "https://qunt-edge.vercel.app/api/mcp",
      "headers": {
        "Authorization": "Bearer ${env:QUNT_MCP_API_KEY}"
      },
      "description": "Qunt Edge — API key in Authorization header (works in Cursor, OpenCode, Grok). Set QUNT_MCP_API_KEY. Create key at Settings → API Keys."
    },
    "qunt-edge-oauth": {
      "type": "mcp",
      "url": "https://qunt-edge.vercel.app/api/mcp/oauth",
      "description": "Experimental OAuth only — use qunt-edge with API key instead"
    },
    "qunt-edge-public": {
      "type": "mcp",
      "url": "https://qunt-edge.vercel.app/api/mcp/public",
      "description": "Public prop firms, deals, blog (no auth)"
    }
  }
}