import { MarketingPricingSection } from '@/components/layout/marketing-pricing-section'

export default async function PricingSection({ locale }: { locale: string }) {
  return <MarketingPricingSection locale={locale} />
}
