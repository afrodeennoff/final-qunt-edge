/* Intentionally empty — the home page is a server component that streams
 * directly.  We deliberately provide NO loading shell so the user never
 * sees an intermediate spinner / skeleton state. */
export default function HomeLoading() {
  return null
}
