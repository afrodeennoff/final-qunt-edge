---
wave: 1
depends_on: ['01']
files_modified:
  - components/patterns/stat-card.tsx
  - components/patterns/hero-card.tsx
  - components/patterns/section-card.tsx
  - components/patterns/chart-card.tsx
  - components/patterns/donut-chart-card.tsx
  - components/patterns/briefing-carousel.tsx
  - components/patterns/ranked-list.tsx
  - components/patterns/empty-state.tsx
  - components/patterns/page-shell.tsx
  - components/patterns/page-content.tsx
  - components/patterns/top-bar.tsx
  - components/patterns/bottom-nav.tsx
  - components/patterns/list-item.tsx (NEW)
requirements:
  - REQ-VISUAL-001
  - REQ-VISUAL-002
  - REQ-VISUAL-005
autonomous: true
must_haves:
  - All 12 pattern components use semantic tokens (no hardcoded hex)
  - Number+unit ratio follows StyleSeed Rule 2 (2:1, ms-0.5 gap, whitespace-nowrap)
  - Labels use 12px uppercase tracking-[0.05em]
  - Card borders use border-[hsl(var(--border))/_0.18]
  - New list-item.tsx created
---

# Plan 02: Pattern Components — Dark Theme & StyleSeed Rules

## Task 1: Update StatCard with dark theme and StyleSeed Rule 2

<objective>Ensure StatCard uses dark-compatible semantic tokens and 2:1 number/unit ratio</objective>

<read_first>
- components/patterns/stat-card.tsx — current implementation using light-theme text tokens
</read_first>

<action>
1. In `components/patterns/stat-card.tsx`, update className values:

- Icon badge: `bg-primary/10` — already correct (key color only in 10% bg)
- Icon: `text-primary` — already correct
- Label: change `text-text-secondary` → `text-muted-foreground` (Tailwind semantic token, works in dark)
- Metric value: change `text-[36px] text-text-primary` → `text-[36px] text-foreground leading-none` (use semantic `text-foreground`)
- Unit: change `text-[18px] text-text-secondary` → `text-[18px] text-muted-foreground` (2:1 ratio preserved)
- Trend up: `text-success` — already correct
- Trend down: `text-destructive` — already correct
- Trend label: change `text-text-tertiary` → `text-muted-foreground`

2. Add `whitespace-nowrap` to the value/unit flex container:
```
<div className="flex items-baseline whitespace-nowrap">
```

3. Remove any `#` hex color references (there are none currently, verify).
</action>

<acceptance_criteria>
- `rg "text-text-primary" components/patterns/stat-card.tsx` returns nothing
- `rg "text-foreground" components/patterns/stat-card.tsx` returns a match
- `rg "whitespace-nowrap" components/patterns/stat-card.tsx` returns a match
- `rg "#[0-9a-fA-F]{3,6}" components/patterns/stat-card.tsx` returns nothing
- `npm run typecheck` passes
</acceptance_criteria>

## Task 2: Update HeroCard with dark theme compliance

<objective>Apply dark-theme semantic tokens to HeroCard; enforce watermark at 6% opacity</objective>

<read_first>
- components/patterns/hero-card.tsx — current implementation
</read_first>

<action>
1. Update className values in `components/patterns/hero-card.tsx`:

- Outer card: `rounded-2xl p-8 bg-card shadow-card` — keep `bg-card` (maps to `--card`)
- Add border: `border border-[hsl(var(--border)/0.18)]` to the outer card
- Watermark: `opacity-[0.06]` — already correct
- Icon badge: `bg-primary/10` — correct
- Label: change `text-text-secondary` → `text-muted-foreground`
- Metric value (48px): change `text-text-primary` → `text-foreground`
- Unit (24px): change `text-text-secondary` → `text-muted-foreground`
- Trend label: change `text-text-tertiary` → `text-muted-foreground`
- Add `whitespace-nowrap` to the value/unit flex container
</action>

<acceptance_criteria>
- `rg "text-text-primary" components/patterns/hero-card.tsx` returns nothing
- `rg "text-foreground" components/patterns/hero-card.tsx` returns a match
- `rg "border-\\[hsl" components/patterns/hero-card.tsx` returns a match
- `npm run typecheck` passes
</acceptance_criteria>

## Task 3: Update SectionCard with dark border and proper shadow

<objective>Add frost border to SectionCard, ensure dark bg token usage</objective>

<read_first>
- components/patterns/section-card.tsx — current implementation
</read_first>

<action>
1. In `components/patterns/section-card.tsx`, update the outer card className:

```
'rounded-2xl p-6 bg-card shadow-card border border-[hsl(var(--border)/0.18)]'
```

2. Update title: change `text-text-primary` → `text-foreground`
</action>

<acceptance_criteria>
- `rg "border-\\[hsl" components/patterns/section-card.tsx` returns a match
- `rg "text-text-primary" components/patterns/section-card.tsx` returns nothing
- `npm run typecheck` passes
</acceptance_criteria>

## Task 4: Update ChartCard with dark tokens and StyleSeed period toggle

<objective>ChartCard period toggle uses subdued pill system; stats use semantic tokens</objective>

<read_first>
- components/patterns/chart-card.tsx — current implementation
</read_first>

<action>
1. In `components/patterns/chart-card.tsx`:

- Outer card: add `border border-[hsl(var(--border)/0.18)]`
- Title: change `text-text-primary` → `text-foreground`
- Period toggle container: change `bg-surface-muted` → `bg-muted`
- Active period button: change `bg-primary text-white` → `bg-primary text-primary-foreground`
- Inactive period: change `text-text-disabled hover:text-text-secondary` → `text-muted-foreground hover:text-foreground`
- Stats border: change `border-surface-muted` → `border-border`
- Stat labels: change `text-text-tertiary` → `text-muted-foreground`
- Stat values: change `text-text-primary` → `text-foreground`
- Stat units: change `text-text-secondary` → `text-muted-foreground`
</action>

<acceptance_criteria>
- `rg "text-text-" components/patterns/chart-card.tsx` returns nothing
- `rg "bg-muted" components/patterns/chart-card.tsx` returns a match
- `rg "text-foreground" components/patterns/chart-card.tsx` returns a match
- `npm run typecheck` passes
</acceptance_criteria>

## Task 5: Update DonutChartCard, BriefingCarousel, RankedList, EmptyState, TopBar, BottomNav with dark tokens

<objective>Replace all `text-text-*` and `bg-surface-*` with Tailwind semantic tokens across remaining pattern components</objective>

<read_first>
- components/patterns/donut-chart-card.tsx
- components/patterns/briefing-carousel.tsx
- components/patterns/ranked-list.tsx
- components/patterns/empty-state.tsx
- components/patterns/top-bar.tsx
- components/patterns/bottom-nav.tsx
</read_first>

<action>
For each file, apply these substitutions:

**donut-chart-card.tsx:**
- `text-text-primary` → `text-foreground`
- `text-text-secondary` → `text-muted-foreground`
- `text-text-tertiary` → `text-muted-foreground`
- `border-surface-muted` → `border-border`
- `bg-surface-subtle` → `bg-muted/50`
- `bg-surface-muted` → `bg-muted`
- Add `border border-[hsl(var(--border)/0.18)]` to outer card
- Remove inline `style={{ backgroundColor: item.color || '#888' }}` — use `bg-chart-1` through `bg-chart-5` or a passed className prop

**briefing-carousel.tsx:**
- `text-text-primary` → `text-foreground`
- `text-text-secondary` → `text-muted-foreground`
- `text-text-tertiary` → `text-muted-foreground`
- `text-destructive` — keep (semantic)
- Add `border border-[hsl(var(--border)/0.18)]` to each carousel card
- Remove inline `style={{ color: item.badgeColor }}` — use a className prop or conditional text color

**ranked-list.tsx:**
- `text-text-primary` → `text-foreground`
- `text-text-tertiary` → `text-muted-foreground`
- `bg-brand-tint` → `bg-primary/8`
- `bg-surface-subtle` → `bg-muted/50`
- `bg-surface-muted` → `bg-muted`
- `border-surface-muted` → `border-border`

**empty-state.tsx:**
- `bg-surface-muted` → `bg-muted`
- `text-text-tertiary` → `text-muted-foreground`
- `text-text-secondary` → `text-muted-foreground`

**top-bar.tsx:**
- `text-text-secondary` → `text-muted-foreground`

**bottom-nav.tsx:**
- `text-text-disabled` → `text-muted-foreground/60`
- `text-primary` — keep (active state, correct per StyleSeed key color rule)
- `border-border` — already correct
</action>

<acceptance_criteria>
- `rg "text-text-" components/patterns/donut-chart-card.tsx components/patterns/briefing-carousel.tsx components/patterns/ranked-list.tsx components/patterns/empty-state.tsx components/patterns/top-bar.tsx components/patterns/bottom-nav.tsx` returns nothing
- `rg "style=\\{" components/patterns/donut-chart-card.tsx` returns nothing (inline styles removed)
- `rg "style=\\{" components/patterns/briefing-carousel.tsx` returns nothing
- `npm run typecheck` passes
</acceptance_criteria>

## Task 6: Update PageShell and PageContent for dark theme

<objective>Ensure page shell components use correct max-widths and spacing for dark theme</objective>

<read_first>
- components/patterns/page-shell.tsx
- components/patterns/page-content.tsx
</read_first>

<action>
1. `page-shell.tsx` — no token changes needed. Verify:
- `max-w-[430px]` for mobile — correct
- `max-w-[1320px]` for marketing — correct
- `pb-24` — correct (bottom padding for mobile nav)

2. `page-content.tsx` — verify `space-y-6` section gap is present. Already correct.

No changes needed unless token references exist.
</action>

<acceptance_criteria>
- `rg "text-text-" components/patterns/page-shell.tsx components/patterns/page-content.tsx` returns nothing
- `rg "space-y-6" components/patterns/page-content.tsx` returns a match
</acceptance_criteria>

## Task 7: Create ListItem pattern component

<objective>Create the missing `components/patterns/list-item.tsx` — a reusable row with leading icon, label, value, and optional trailing element</objective>

<read_first>
- components/patterns/stat-card.tsx — reference for pattern conventions
- components/patterns/utils.ts — cn utility
</read_first>

<action>
1. Create `components/patterns/list-item.tsx`:

```tsx
import { cn } from './utils'
import { LucideIcon } from 'lucide-react'
import React from 'react'

interface ListItemProps {
  icon: LucideIcon
  label: string
  value: string
  subtitle?: string
  trailing?: React.ReactNode
  isActive?: boolean
  className?: string
}

export function ListItem({
  icon: Icon,
  label,
  value,
  subtitle,
  trailing,
  isActive = false,
  className,
}: ListItemProps) {
  return (
    <div
      className={cn(
        'flex items-center gap-3 p-3 rounded-xl transition-colors',
        isActive ? 'bg-primary/8 border border-primary/20' : 'bg-muted/50',
        className
      )}
      data-slot="list-item"
    >
      <div className={cn(
        'size-8 rounded-lg flex items-center justify-center',
        isActive ? 'bg-primary/15' : 'bg-muted'
      )}>
        <Icon className={cn('size-4', isActive ? 'text-primary' : 'text-muted-foreground')} strokeWidth={2} />
      </div>
      <div className="flex-1 min-w-0">
        <p className={cn(
          'text-sm font-semibold truncate',
          isActive ? 'text-primary' : 'text-foreground'
        )}>
          {label}
        </p>
        {subtitle && (
          <p className="text-xs text-muted-foreground truncate">{subtitle}</p>
        )}
      </div>
      <span className={cn(
        'text-sm font-bold whitespace-nowrap',
        isActive ? 'text-primary' : 'text-foreground'
      )}>
        {value}
      </span>
      {trailing}
    </div>
  )
}
```
</action>

<acceptance_criteria>
- File `components/patterns/list-item.tsx` exists
- `rg "data-slot=\"list-item\"" components/patterns/list-item.tsx` returns a match
- `rg "text-text-" components/patterns/list-item.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" components/patterns/list-item.tsx` returns nothing
- `npm run typecheck` passes
</acceptance_criteria>
