# 01-VISUAL-REFRESH — Plan 03: Remaining Landing Pages + Dashboard + Auth

---
phase: "01"
plan: "03"
type: "frontend"
wave: 3
depends_on:
  - "02"
files_modified:
  - "app/[locale]/(landing)/terms/page.tsx"
  - "app/[locale]/(landing)/privacy/page.tsx"
  - "app/[locale]/(landing)/disclaimers/page.tsx"
  - "app/[locale]/(landing)/faq/page.tsx"
  - "app/[locale]/(landing)/components/faq.tsx"
  - "app/[locale]/(landing)/docs/page.tsx"
  - "app/[locale]/(landing)/support/page.tsx"
  - "app/[locale]/(landing)/about/page.tsx"
  - "app/[locale]/(landing)/community/page.tsx"
  - "app/[locale]/(landing)/blogs/page.tsx"
  - "app/[locale]/(landing)/newsletter/page.tsx"
  - "app/[locale]/(landing)/referral/page.tsx"
  - "app/[locale]/(landing)/leaderboard/page.tsx"
  - "app/[locale]/(landing)/best-trading-journal/page.tsx"
  - "app/[locale]/(landing)/firm/page.tsx"
  - "app/[locale]/(landing)/updates/page.tsx"
  - "app/[locale]/(landing)/maintenance/page.tsx"
  - "app/[locale]/dashboard/components/charts/equity-chart.tsx"
  - "app/[locale]/dashboard/components/charts/pnl-bar-chart.tsx"
  - "app/[locale]/dashboard/components/charts/weekday-pnl.tsx"
  - "app/[locale]/dashboard/components/charts/time-in-position.tsx"
  - "app/[locale]/dashboard/components/statistics/trade-performance-card.tsx"
  - "app/[locale]/dashboard/components/statistics/*.tsx"
  - "components/ui/chart-surface.tsx"
  - "components/ui/widget-shell.tsx"
  - "app/[locale]/dashboard/components/widget-canvas.tsx"
  - "app/[locale]/(authentication)/authentication/page-client.tsx"
  - "app/[locale]/(authentication)/components/user-auth-form.tsx"
files_created: []
autonomous: false
requirements_addressed:
  - "REQ-VISUAL-003"
  - "REQ-VISUAL-004"
  - "REQ-VISUAL-005"
---

---

## <objective>

Apply StyleSeed section types and patterns to all remaining landing pages (terms, privacy, faq, docs, support, about, blogs, community, leaderboard, newsletter, referral, best-trading-journal, firm, disclaimers, updates, maintenance). Then apply StyleSeed visual rules to the dashboard (charts, stat cards, widget chrome) and auth pages (two-column layout, form patterns).

---

## <read_first>

- `docs/superpowers/specs/2026-04-08-visual-refresh-design.md`
- `engine/DESIGN-LANGUAGE.md` — Rules 14, 15, 16, 17, 18, 19, 21
- `app/[locale]/(landing)/terms/page.tsx`
- `app/[locale]/(landing)/faq/page.tsx`
- `app/[locale]/(landing)/docs/page.tsx`
- `app/[locale]/(landing)/support/page.tsx`
- `app/[locale]/(landing)/about/page.tsx`
- `app/[locale]/(landing)/blogs/page.tsx`
- `app/[locale]/(landing)/community/page.tsx`
- `app/[locale]/(landing)/leaderboard/page.tsx`
- `app/[locale]/(landing)/newsletter/page.tsx`
- `app/[locale]/(landing)/referral/page.tsx`
- `app/[locale]/(landing)/best-trading-journal/page.tsx`
- `app/[locale]/(landing)/firm/page.tsx`
- `app/[locale]/(landing)/disclaimers/page.tsx`
- `app/[locale]/(landing)/updates/page.tsx`
- `app/[locale]/(landing)/maintenance/page.tsx`
- `app/[locale]/dashboard/AGENTS.md`
- `app/[locale]/dashboard/components/charts/equity-chart.tsx` (first 200 lines)
- `components/ui/chart-surface.tsx`
- `components/ui/widget-shell.tsx`
- `app/[locale]/dashboard/components/statistics/trade-performance-card.tsx`
- `app/[locale]/(authentication)/authentication/page-client.tsx`
- `app/[locale]/(authentication)/components/user-auth-form.tsx`
- `components/ui/v2/card-v2.tsx`
- `components/ui/v2/button-v2.tsx`
- `components/ui/v2/input-v2.tsx`

---

## <tasks>

### 18. Remaining landing pages — terms, privacy, disclaimers

**Files**: `terms/page.tsx`, `privacy/page.tsx`, `disclaimers/page.tsx`  
**Action**: Apply minimal Type A section card to legal content pages:
- Wrap main content in `SectionCard` or `mx-6 rounded-2xl p-6 bg-card`
- Page title: 24px bold → `text-2xl font-bold`
- Body text: 14-15px → StyleSeed body scale
- Apply `space-y-6` between content sections
- Links in text: `text-primary hover:underline`
- No charts or metrics on these pages

**Acceptance criteria**:
- [x] Legal pages wrapped in `rounded-2xl p-6 bg-card mx-6`
- [x] `space-y-6` section gaps
- [x] `npm run typecheck` passes

---

### 19. FAQ page — Accordion with SectionCard

**Files**: `faq/page.tsx`, `app/[locale]/(landing)/components/faq.tsx`  
**Action**: If not already done in Plan 02:
- Wrap in SectionCard title="Frequently Asked Questions"
- Accordion items: `rounded-lg bg-surface-muted p-4`
- Item header: question text + chevron icon
- Item content: answer text in `text-text-secondary`
- No dividers between items — use `space-y-2` within accordion container
- Apply blur entrance animation to FAQ items on scroll

**Acceptance criteria**:
- [x] FAQ uses SectionCard wrapper
- [x] Accordion items styled with surface-muted background
- [x] Blur entrance animation on scroll

---

### 20. Docs page — Navigation + Content

**Files**: `docs/page.tsx`  
**Action**: Apply StyleSeed patterns to docs layout:
- Sidebar navigation: 14px font-medium, `text-text-secondary`, active `text-primary`
- Main content: 15px body text, generous line-height
- Code blocks: `bg-surface-card rounded-lg p-4 font-mono text-sm`
- Headings: proper hierarchy (h1 → 2xl, h2 → xl, h3 → lg)
- Apply `space-y-6` between sections
- Doc navigation (prev/next): use ButtonV2 outline variant

**Acceptance criteria**:
- [x] Sidebar nav uses proper text hierarchy
- [x] Code blocks styled with surface background
- [x] `space-y-6` section gaps

---

### 21. Support + About + Community pages

**Files**: `support/page.tsx`, `about/page.tsx`, `community/page.tsx`  
**Action**: Read each page, apply Type A/B patterns:
- Support: contact form or info cards → Type B grid
- About: team/company info → Type A card sections
- Community: member cards or posts → Type B grid or Type C carousel
- All: `space-y-6` between sections, `mx-6` or `px-6` wrapper
- Community posts: use `RankedList` pattern if ranking

**Acceptance criteria**:
- [x] Each page uses correct section type (A/B/C)
- [x] `space-y-6` gaps throughout
- [x] `npm run typecheck` passes

---

### 22. Blogs + Newsletter + Referral pages

**Files**: `blogs/page.tsx`, `newsletter/page.tsx`, `referral/page.tsx`  
**Action**: Read each page:
- Blogs: article cards in grid → Type B (`px-6`), each card `rounded-2xl p-6 bg-card`
- Article card: image placeholder + title (18px bold) + excerpt (14px) + date (13px tertiary)
- Newsletter: signup form → Type A card, input + CTA button
- Referral: stats cards + share link → Type B grid + StatCard pattern

**Acceptance criteria**:
- [x] Blog cards use Type B grid with rounded-2xl cards
- [x] Newsletter uses Type A card with form
- [x] Referral stats use StatCard pattern
- [x] `npm run typecheck` passes

---

### 23. Leaderboard + Best Trading Journal + Firm pages

**Files**: `leaderboard/page.tsx`, `best-trading-journal/page.tsx`, `firm/page.tsx`  
**Action**: Read each page:
- Leaderboard: ranked list → use `RankedList` pattern component
  - Top 3: highlighted with `bg-brand-tint border-2 border-brand`
  - Ranks 4+: normal rows with `bg-surface-subtle`
  - Stats per row: number+unit with 2:1 ratio
- Best Trading Journal: landing page → apply Type A/B sections
- Firm: detail page → similar to propfirm detail

**Acceptance criteria**:
- [x] Leaderboard uses RankedList pattern with highlighted rows
- [x] Stats use 2:1 number+unit ratio
- [x] `npm run typecheck` passes

---

### 24. Updates + Maintenance pages

**Files**: `updates/page.tsx`, `updates/_updates/page.tsx`, `maintenance/page.tsx`  
**Action**: Read each page:
- Updates/changelog: timeline list → Type A card with `border-t` dividers
- Each update: version badge + date + change description
- Maintenance: status page → Type A card, centered content, status indicator
- Apply `space-y-6` between update items

**Acceptance criteria**:
- [x] Updates use Type A card with timeline structure
- [x] Maintenance page uses centered Type A card
- [x] `npm run typecheck` passes

---

### 25. Dashboard charts — StyleSeed Rule 17

**Files**: `app/[locale]/dashboard/components/charts/equity-chart.tsx`, `pnl-bar-chart.tsx`, `weekday-pnl.tsx`, `time-in-position.tsx`  
**Action**: Apply StyleSeed chart rules to all area/bar charts:
- Area chart line: `strokeWidth={2.5}`, color `var(--primary)` (brand key color)
- Gradient fill: key color 15% opacity at top → 0% at bottom
- No dots on data points
- X-axis: hidden (no axis line, no tick labels)
- Y-axis: completely hidden
- Chart height: `h-40` (160px) for area, `h-44` (176px) for bar
- Margin: `-mx-2` for breathing room

Read each chart component and apply the above rules. The existing charts may already be close — only fix violations.

**Acceptance criteria**:
- [x] All area charts use strokeWidth 2.5
- [x] All area charts have gradient fill (15% → 0%)
- [x] All chart axes are hidden
- [x] No dots on area chart data points
- [x] `npm run typecheck` passes

---

### 26. Dashboard stat cards — StyleSeed Rule 2

**Files**: `app/[locale]/dashboard/components/statistics/*.tsx`  
**Action**: Apply 2:1 number+unit ratio to all stat displays:
- Metric value: bold, size appropriate to context (18-36px)
- Unit: 50% size of metric, attached with `ms-0.5`
- ALWAYS use `whitespace-nowrap` on number+unit combos
- Label: 12px uppercase `tracking-wide text-text-secondary`
- Trend: same color as icon, share `text-success` or `text-destructive`
- Icon badge: 28px container, 10% opacity background, 16px icon

Read `trade-performance-card.tsx` and any other stat components. Apply the ratio rule to all metric displays.

**Acceptance criteria**:
- [x] All stat values have attached units at 50% size
- [x] All number+unit combos use `whitespace-nowrap`
- [x] All labels are 12px uppercase
- [x] `npm run typecheck` passes

---

### 27. Dashboard widget chrome refinements

**Files**: `components/ui/chart-surface.tsx`, `components/ui/widget-shell.tsx`, `app/[locale]/dashboard/components/widget-canvas.tsx`  
**Action**: Minor refinements to existing widget system:
- Verify `chart-surface.tsx` border uses `border-v2-border/16` (already aligned)
- Verify background uses `bg-v2-bg-surface/88` (already aligned)
- Widget title: 15px font-semibold (already aligned)
- Verify no stacked frames — widgets use borderless surfaces (already aligned)
- Bottom stats grid: if any widget has stats below a chart, add `border-t border-surface-muted pt-5`
- Verify widget animations use `duration-normal` (200ms) not hardcoded values

**Acceptance criteria**:
- [x] All widget surfaces use correct border/background tokens
- [x] No stacked widget frames
- [x] Chart+stats widgets have border-t divider
- [x] Widget animations use duration-200

---

### 28. Auth pages — Type A two-column card + form

**Files**: `app/[locale]/(authentication)/authentication/page-client.tsx`, `user-auth-form.tsx`  
**Action**: Apply StyleSeed patterns to auth pages:
- Two-column layout preserved but refined:
  - Left panel: branding + value props in `rounded-2xl p-8 bg-card` card
  - Right panel: form in `rounded-2xl p-6 bg-card/70 backdrop-blur`
  - Both panels inside one `rounded-2xl overflow-hidden` container
- Input styling: use `InputV2` component
  - Height: `h-10` (40px)
  - Border: `border-v2-border`
  - Focus: `focus-visible:ring-2 focus-visible:ring-v2-accent`
  - Error: `border-v2-error`
- Button: use `ButtonV2` primary with `rounded-xl`
  - Hover: `hover:scale-[1.01]`
- OAuth buttons: use `ButtonV2` outline with icon + text
  - Border: `border-v2-border`
  - Background: `bg-card/50`
  - Hover: `bg-accent/70`
- Error messages: use `text-destructive` color, 13px
- Apply blur entrance animation to the two-column card

**Acceptance criteria**:
- [x] Two-column card uses rounded-2xl overflow-hidden container
- [x] Inputs use InputV2 with correct focus/error states
- [x] Buttons use ButtonV2 with hover effects
- [x] OAuth buttons styled with outline variant
- [x] Blur entrance animation on page load
- [x] `npm run typecheck` passes

---

## <verification>

1. Run `npm run typecheck` — all modified files pass TypeScript
2. Run `npm run lint` — no new lint errors
3. Visual QA: all ~15 remaining landing pages — verify section patterns
4. Visual QA: `/dashboard` — verify chart styling and stat ratios
5. Visual QA: `/auth` — verify two-column card and form styling
6. Anti-pattern sweep: `grep -r "#[0-9a-fA-F]{6}" app/\[locale\]/\(landing\)/ --include="*.tsx" | grep -v "oklch\|hsl\|var\|CSS"` — check for hardcoded hex that should use CSS variables

---

## <success_criteria>

- [x] All ~15 remaining landing pages use `space-y-6` gaps
- [x] All landing pages use `mx-6` or `px-6` wrapper discipline
- [x] Dashboard charts follow Rule 17 styling
- [x] Dashboard stat cards use 2:1 number+unit ratio
- [x] Dashboard widget chrome uses correct tokens
- [x] Auth pages use refined Type A card + InputV2 + ButtonV2
- [x] OAuth buttons use ButtonV2 outline variant
- [x] Auth page has blur entrance animation
- [x] No inline hex colors in any modified landing/dashboard/auth files
- [x] `npm run typecheck` passes
- [x] `npm run lint` passes
