---
wave: 3
depends_on: ['01', '02']
files_modified:
  - components/ui/chart-surface.tsx
  - components/ui/chart-surface-enhanced.tsx
  - app/[locale]/dashboard/components/charts/equity-chart.tsx
  - app/[locale]/dashboard/components/charts/pnl-per-contract.tsx
  - app/[locale]/dashboard/components/charts/pnl-bar-chart.tsx
  - app/[locale]/dashboard/components/charts/pnl-time-bar-chart.tsx
  - app/[locale]/dashboard/components/charts/pnl-per-contract-daily.tsx
  - app/[locale]/dashboard/components/charts/pnl-by-side.tsx
  - app/[locale]/dashboard/components/charts/trade-distribution.tsx
  - app/[locale]/dashboard/components/charts/weekday-pnl.tsx
  - app/[locale]/dashboard/components/charts/time-in-position.tsx
  - app/[locale]/dashboard/components/charts/time-range-performance.tsx
  - app/[locale]/dashboard/components/charts/contract-quantity.tsx
  - app/[locale]/dashboard/components/charts/commissions-pnl.tsx
  - app/[locale]/dashboard/components/charts/tick-distribution.tsx
  - app/[locale]/dashboard/components/charts/daily-tick-target.tsx
  - app/[locale]/dashboard/components/widget-canvas.tsx
requirements:
  - REQ-VISUAL-002
  - REQ-VISUAL-004
autonomous: true
must_haves:
  - Area charts use StyleSeed Rule 17 (strokeWidth 2.5, gradient 15%→0%, hidden axes)
  - Bar charts use consistent dark-theme colors
  - Chart surfaces use dark bg with frost border
  - Widget chrome is visually transparent in normal mode
  - Skeleton/loading states use dark-compatible shimmer
---

# Plan 06: Dashboard Charts — StyleSeed Rule 17 & Widget Chrome

## Task 1: Apply StyleSeed Rule 17 to chart-surface.tsx

<objective>Chart surface wrapper applies dark card styling with frost border, proper loading states</objective>

<read_first>
- components/ui/chart-surface.tsx — current chart wrapper (186 lines)
</read_first>

<action>
1. Read the full `chart-surface.tsx` file (offset 61-186).

2. Update the chart card wrapper className to include frost border:
   - Add `border border-[hsl(var(--border)/0.18)]` to the outer card container
   - Ensure `bg-card` is used for the card background
   - Skeleton shimmer: verify it uses `from-transparent via-[hsl(var(--foreground)/_0.03)]` — if hardcoded, update to semantic token

3. Update skeleton colors to dark-compatible:
   - Replace any `bg-gray-200` or similar light skeleton → `bg-muted`
   - Shimmer gradient: `from-transparent via-foreground/3 to-transparent`

4. Verify `emptyMessage` and `errorMessage` states use `text-muted-foreground`.
</action>

<acceptance_criteria>
- `rg "border-\\[hsl" components/ui/chart-surface.tsx` returns a match
- `rg "bg-gray" components/ui/chart-surface.tsx` returns nothing
- `rg "bg-muted" components/ui/chart-surface.tsx` returns at least one match
- `npm run typecheck` passes
</acceptance_criteria>

## Task 2: Apply StyleSeed Rule 17 to area charts (equity-chart, pnl-per-contract)

<objective>Area charts use strokeWidth 2.5, gradient fill 15%→0%, hidden axes, stroke color from semantic token</objective>

<read_first>
- app/[locale]/dashboard/components/charts/equity-chart.tsx
- app/[locale]/dashboard/components/charts/pnl-per-contract.tsx
- app/[locale]/dashboard/components/charts/pnl-per-contract-daily.tsx
</read_first>

<action>
1. For each area chart file, ensure the `<Area>` component has:
```tsx
<Area
  type="monotone"
  dataKey="value"
  stroke="hsl(var(--primary))"
  strokeWidth={2.5}
  fill="url(#areaGradient)"
  fillOpacity={1}
/>
```

2. Ensure a gradient definition exists:
```tsx
<defs>
  <linearGradient id="areaGradient" x1="0" y1="0" x2="0" y2="1">
    <stop offset="0%" stopColor="hsl(var(--primary))" stopOpacity={0.15} />
    <stop offset="100%" stopColor="hsl(var(--primary))" stopOpacity={0} />
  </linearGradient>
</defs>
```

3. Hide or minimize axes per StyleSeed Rule 17:
   - `<XAxis>`: `tick={false} axisLine={false} tickLine={false}` or remove entirely
   - `<YAxis>`: `tick={false} axisLine={false} tickLine={false}` or remove entirely
   - `<CartesianGrid>`: `strokeDasharray="3 3" stroke="hsl(var(--border))" opacity={0.3}` or remove

4. Tooltip: use `ChartTooltip` component or dark-compatible tooltip with `bg-card border border-[hsl(var(--border)/0.18)] text-foreground`

5. Replace any hardcoded stroke/fill colors (`#3b82f6`, `blue-500`, etc.) with `hsl(var(--primary))` or `hsl(var(--chart-N))`.
</action>

<acceptance_criteria>
- `rg "strokeWidth.*2\\.5" app/[locale]/dashboard/components/charts/equity-chart.tsx` returns a match
- `rg "stopOpacity.*0\\.15" app/[locale]/dashboard/components/charts/equity-chart.tsx` returns a match
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/dashboard/components/charts/equity-chart.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/dashboard/components/charts/pnl-per-contract.tsx` returns nothing
- `npm run typecheck` passes
</acceptance_criteria>

## Task 3: Apply dark theme to bar charts and remaining chart types

<objective>All bar charts, distribution charts, and specialty charts use dark semantic tokens</objective>

<read_first>
- app/[locale]/dashboard/components/charts/pnl-bar-chart.tsx
- app/[locale]/dashboard/components/charts/pnl-time-bar-chart.tsx
- app/[locale]/dashboard/components/charts/pnl-by-side.tsx
- app/[locale]/dashboard/components/charts/trade-distribution.tsx
- app/[locale]/dashboard/components/charts/weekday-pnl.tsx
- app/[locale]/dashboard/components/charts/time-in-position.tsx
- app/[locale]/dashboard/components/charts/time-range-performance.tsx
- app/[locale]/dashboard/components/charts/contract-quantity.tsx
- app/[locale]/dashboard/components/charts/commissions-pnl.tsx
- app/[locale]/dashboard/components/charts/tick-distribution.tsx
- app/[locale]/dashboard/components/charts/daily-tick-target.tsx
</read_first>

<action>
1. For each chart file:
   - Replace hardcoded bar fill colors (`#22c55e`, `#ef4444`, etc.) with semantic tokens:
     - Positive/green: `hsl(var(--success))`
     - Negative/red: `hsl(var(--destructive))`
     - Neutral: `hsl(var(--primary))`
     - Chart series: `hsl(var(--chart-1))` through `hsl(var(--chart-5))`
   - Bar stroke: `strokeWidth={0}` or `strokeWidth={1}` with `stroke="hsl(var(--border))"`
   - CartesianGrid: `stroke="hsl(var(--border))" opacity={0.3}`
   - Axes: `tick={false}` or minimal tick styling with `fill="hsl(var(--muted-foreground))"`
   - Tooltip: dark-compatible bg

2. Ensure no `bg-white`, `text-black`, or other light-theme artifacts.
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/dashboard/components/charts/pnl-bar-chart.tsx app/[locale]/(locale)/dashboard/components/charts/pnl-time-bar-chart.tsx` returns nothing (run for all 11 files)
- Each file references `hsl(var(--` for color values
- `npm run typecheck` passes
</acceptance_criteria>

## Task 4: Update widget-canvas.tsx chrome to be visually transparent in normal mode

<objective>Widget chrome (borders, backgrounds) in normal mode is transparent per AGENTS.md convention</objective>

<read_first>
- app/[locale]/dashboard/components/widget-canvas.tsx
</read_first>

<action>
1. Read `widget-canvas.tsx`.

2. In normal mode (non-customize), ensure widget wrappers have:
   - No visible border or background (transparent chrome)
   - Only the widget surface component (`WidgetShell`, `ChartSurface`, `StatsCard`, `Card`) owns borders/backgrounds
   - Remove any `border-border` or `bg-card` from the widget wrapper in normal mode

3. In customize mode, outer shell chrome is acceptable (drag handles, selection borders).

4. Ensure any grid gap uses `gap-4` (consistent with Type B grid).

5. Remove any hardcoded hex colors.
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/dashboard/components/widget-canvas.tsx` returns nothing
- In normal mode, widget wrapper has no `border` or `bg-card` class (chrome is transparent)
- `npm run typecheck` passes
</acceptance_criteria>
