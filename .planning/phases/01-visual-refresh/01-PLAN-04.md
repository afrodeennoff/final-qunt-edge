---
wave: 2
depends_on: ['01', '02', '03']
files_modified:
  - app/[locale]/(home)/components/Hero.tsx
  - app/[locale]/(home)/components/FeaturesBento.tsx
  - app/[locale]/(home)/components/HowItWorks.tsx
  - app/[locale]/(home)/components/SocialProof.tsx
  - app/[locale]/(home)/components/TrustAndProof.tsx
  - app/[locale]/(home)/components/AIFeatures.tsx
  - app/[locale]/(home)/components/ProblemStatement.tsx
  - app/[locale]/(home)/components/PricingSection.tsx
  - app/[locale]/(home)/components/FinalCTA.tsx
  - app/[locale]/(home)/components/FAQSection.tsx
  - app/[locale]/(home)/components/DashboardPreview.tsx
  - app/[locale]/(home)/components/WhyChooseUs.tsx
  - app/[locale]/(home)/components/ComparisonSection.tsx
  - app/[locale]/(home)/components/LiveStatsStrip.tsx
  - app/[locale]/(home)/components/Navigation.tsx
  - app/[locale]/(home)/components/Footer.tsx
  - app/[locale]/(home)/components/RollingAdBanner.tsx
  - app/[locale]/(home)/page.tsx
requirements:
  - REQ-VISUAL-001
  - REQ-VISUAL-005
autonomous: true
must_haves:
  - Hero section uses Type D layout (dark bg, no light/dark inconsistency)
  - All sections use space-y-6 gap
  - No hardcoded hex colors
  - Responsive improvements for mobile/tablet
  - MotionStagger/MotionSection used for scroll reveals
---

# Plan 04: Homepage — Section Type System & Dark Theme Fix

## Task 1: Fix Hero dark theme inconsistency

<objective>Remove any light-theme artifacts in the Hero component; ensure consistent dark bg</objective>

<read_first>
- app/[locale]/(home)/components/Hero.tsx — current hero (198 lines)
</read_first>

<action>
1. Read the full Hero.tsx file. Look for:
   - Any `bg-white`, `bg-background` that resolves to light in certain contexts
   - Any `text-gray-*`, `text-slate-*` hardcoded colors
   - Mixed light/dark className patterns

2. Ensure the hero section uses:
   - `bg-background` (maps to dark `--background: oklch(0 0 0)`)
   - `text-foreground` for primary text
   - `text-muted-foreground` for secondary text
   - Badge: `border-[var(--frost-border)]` — already present, keep it
   - No inline hex colors

3. If the Hero has a light overlay or gradient that creates visual inconsistency with the dark shell, replace with dark-compatible alternatives:
   - Replace any `from-white` gradients → `from-background`
   - Replace any `bg-white/xx` → `bg-card/xx` or `bg-muted/xx`
   - Replace any `text-black` → `text-foreground`

4. Verify the `capabilityCards` gradient tones use `hsl(var(--...))` format — they already do per the file content. Keep them.

5. Ensure CTA buttons use:
   - Primary: `bg-primary text-primary-foreground`
   - Secondary: `bg-secondary text-secondary-foreground` or `border border-[hsl(var(--border)/0.18)]`
</action>

<acceptance_criteria>
- `rg "bg-white" app/[locale]/(home)/components/Hero.tsx` returns nothing
- `rg "text-black" app/[locale]/(home)/components/Hero.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/Hero.tsx` returns nothing (except in gradient tones like `hsl(var(--...))`)
- `rg "bg-background" app/[locale]/(home)/components/Hero.tsx` returns a match
- `npm run typecheck` passes
</acceptance_criteria>

## Task 2: Apply Type B grid layout to FeaturesBento and WhyChooseUs

<objective>Feature sections use Type B (px-6, grid gap-4) with StyleSeed card borders and semantic tokens</objective>

<read_first>
- app/[locale]/(home)/components/FeaturesBento.tsx
- app/[locale]/(home)/components/WhyChooseUs.tsx
</read_first>

<action>
1. In `FeaturesBento.tsx`:
   - Ensure outer section uses `px-4 sm:px-6 lg:px-8` for responsive padding
   - Card items use `rounded-2xl bg-card shadow-card border border-[hsl(var(--border)/0.18)]`
   - Text colors: `text-foreground` (primary), `text-muted-foreground` (secondary)
   - Icon badges: `bg-primary/10 text-primary` (key color, 10% bg only)
   - Grid gap: `gap-4` between cards
   - Remove any hardcoded hex colors

2. In `WhyChooseUs.tsx`:
   - Same pattern: Type B grid with `gap-4`, responsive `px-*`
   - Cards: `rounded-2xl bg-card shadow-card border border-[hsl(var(--border)/0.18)]`
   - All `text-text-*` → `text-foreground` / `text-muted-foreground`
   - All `bg-surface-*` → `bg-card` / `bg-muted`
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/FeaturesBento.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/WhyChooseUs.tsx` returns nothing
- `rg "rounded-2xl" app/[locale]/(home)/components/FeaturesBento.tsx` returns at least one match
- `npm run typecheck` passes
</acceptance_criteria>

## Task 3: Apply Type A layout to HowItWorks, SocialProof, ProblemStatement, FinalCTA

<objective>Single-card sections use Type A (mx-6, floating card with rounded-2xl)</objective>

<read_first>
- app/[locale]/(home)/components/HowItWorks.tsx
- app/[locale]/(home)/components/SocialProof.tsx
- app/[locale]/(home)/components/ProblemStatement.tsx
- app/[locale]/(home)/components/FinalCTA.tsx
</read_first>

<action>
1. For each component, ensure:
   - Sections wrapped in cards use `rounded-2xl bg-card shadow-card border border-[hsl(var(--border)/0.18)] p-6`
   - Section spacing uses the parent `space-y-6` from the page shell
   - No `text-text-*` tokens — use `text-foreground` / `text-muted-foreground`
   - No `bg-surface-*` — use `bg-card` / `bg-muted`
   - No hardcoded hex colors

2. For `HowItWorks.tsx` specifically:
   - Step indicators use `bg-primary/10 text-primary` (key color badge)
   - Step numbers or icons at 10% bg tint only

3. For `SocialProof.tsx`:
   - Testimonial cards: `rounded-2xl bg-card border border-[hsl(var(--border)/0.18)]`
   - Star ratings: `text-warning` (semantic token)

4. For `FinalCTA.tsx`:
   - CTA section uses dark bg with `bg-primary/5` subtle tint (not solid primary bg)
   - CTA button: `bg-primary text-primary-foreground`

5. For `ProblemStatement.tsx`:
   - Problem cards use `rounded-2xl bg-card border border-[hsl(var(--border)/0.18)]`
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/HowItWorks.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/SocialProof.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/ProblemStatement.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/FinalCTA.tsx` returns nothing
- `rg "rounded-2xl" app/[locale]/(home)/components/HowItWorks.tsx` returns at least one match
- `npm run typecheck` passes
</acceptance_criteria>

## Task 4: Update remaining homepage components for dark theme consistency

<objective>AIFeatures, PricingSection, FAQSection, DashboardPreview, LiveStatsStrip, ComparisonSection all use semantic tokens</objective>

<read_first>
- app/[locale]/(home)/components/AIFeatures.tsx
- app/[locale]/(home)/components/PricingSection.tsx
- app/[locale]/(home)/components/FAQSection.tsx
- app/[locale]/(home)/components/DashboardPreview.tsx
- app/[locale]/(home)/components/LiveStatsStrip.tsx
- app/[locale]/(home)/components/ComparisonSection.tsx
</read_first>

<action>
1. For each file, apply the same token migration:
   - `text-text-*` → `text-foreground` / `text-muted-foreground`
   - `bg-surface-*` → `bg-card` / `bg-muted`
   - Add `border border-[hsl(var(--border)/0.18)]` to card surfaces
   - Remove any hardcoded hex colors (`#[0-9a-f-A-F]{3,6}`)
   - Use `rounded-2xl` for card borders (not arbitrary values)

2. `PricingSection.tsx`:
   - Pricing cards: `rounded-2xl bg-card shadow-card border border-[hsl(var(--border)/0.18)]`
   - Highlighted/featured card: `border-primary` (key color border only on featured)
   - Price values: `text-foreground` with unit at `text-muted-foreground`

3. `DashboardPreview.tsx`:
   - Preview frame: `rounded-2xl bg-card border border-[hsl(var(--border)/0.18)]`
   - Ensure it looks consistent with the dark shell

4. `LiveStatsStrip.tsx`:
   - Stats: use `text-foreground` for values, `text-muted-foreground` for labels
   - Background: `bg-card/50` or `bg-muted/50`
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/AIFeatures.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/PricingSection.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/FAQSection.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/DashboardPreview.tsx` returns nothing
- `npm run typecheck` passes
</acceptance_criteria>

## Task 5: Update Navigation, Footer, RollingAdBanner for dark theme

<objective>Ensure shared navigation components use semantic dark tokens</objective>

<read_first>
- app/[locale]/(home)/components/Navigation.tsx
- app/[locale]/(home)/components/Footer.tsx
- app/[locale]/(home)/components/RollingAdBanner.tsx
</read_first>

<action>
1. For each component:
   - Replace any hardcoded hex colors with semantic tokens
   - `text-text-*` → `text-foreground` / `text-muted-foreground`
   - `bg-surface-*` → `bg-card` / `bg-muted`
   - Ensure `border-border` or `border-[hsl(var(--border)/0.18)]` for dividers
   - Navigation links: `text-muted-foreground hover:text-foreground`
   - Active link: `text-primary` (key color only for active state)

2. `RollingAdBanner.tsx`:
   - Banner bg: `bg-muted/50` or `bg-card`
   - Text: `text-muted-foreground`
   - Border: `border border-[hsl(var(--border)/0.18)]`

3. `Footer.tsx`:
   - Footer bg: `bg-card` or `bg-background` (matches page)
   - Links: `text-muted-foreground hover:text-foreground`
   - Section titles: `text-foreground`
   - Divider: `border-border`
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/Navigation.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/Footer.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(home)/components/RollingAdBanner.tsx` returns nothing
- `npm run typecheck` passes
</acceptance_criteria>

## Task 6: Ensure homepage page.tsx uses space-y-6 section rhythm

<objective>The homepage page component wraps all sections with space-y-6 gap</objective>

<read_first>
- app/[locale]/(home)/page.tsx — main page component
- app/[locale]/(home)/components/HomeContent.tsx — may contain the section layout
</read_first>

<action>
1. Check if the page or HomeContent renders sections. If sections are rendered as direct children:

2. Ensure the container wrapping all `<Hero>`, `<FeaturesBento>`, `<HowItWorks>`, etc. has `space-y-6` class:
```
<div className="space-y-6">
  <Hero />
  <FeaturesBento />
  <HowItWorks />
  ...
</div>
```

3. If sections already have their own margin/gap, remove ad-hoc gaps and rely on `space-y-6` from the parent.

4. Do NOT add `space-y-6` if the marketing-layout-shell already provides it (added in Plan 03 Task 3). Check for double-gapping.
</action>

<acceptance_criteria>
- Either `page.tsx` or `HomeContent.tsx` has `space-y-6` on the section container
- No ad-hoc `mb-16`, `mt-20`, `gap-12` between major sections (use `space-y-6` instead)
- `npm run typecheck` passes
</acceptance_criteria>
