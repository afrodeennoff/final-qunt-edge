---
wave: 3
depends_on: ['01', '02']
files_modified:
  - app/[locale]/dashboard/components/navbar.tsx
  - components/sidebar/dashboard-sidebar.tsx
  - components/ui/unified-sidebar.tsx
  - app/[locale]/(authentication)/components/user-auth-form.tsx
  - app/[locale]/(authentication)/authentication/page-client.tsx
  - app/[locale]/(authentication)/authentication/page-shell.tsx
  - app/[locale]/(authentication)/layout.tsx
  - app/[locale]/(authentication)/layout-shell.tsx
  - app/[locale]/(authentication)/loading.tsx
  - app/[locale]/(authentication)/client-layout.tsx
requirements:
  - REQ-VISUAL-001
  - REQ-VISUAL-002
  - REQ-VISUAL-003
  - REQ-VISUAL-005
autonomous: true
must_haves:
  - Dashboard navbar uses dark theme tokens
  - Dashboard sidebar nav items use StyleSeed active/hover pattern
  - Sidebar contrast uses sidebar-foreground for text
  - Auth pages use Type A two-column card pattern
  - Auth form uses dark semantic tokens
---

# Plan 07: Dashboard Sidebar, Navbar & Auth Pages

## Task 1: Update dashboard navbar for dark theme

<objective>Dashboard top navbar uses dark bg, semantic tokens, subdued pill action buttons</objective>

<read_first>
- app/[locale]/dashboard/components/navbar.tsx
</read_first>

<action>
1. Read the full file.

2. Apply dark theme migration:
   - Navbar bg: `bg-background/80 backdrop-blur-md border-b border-border`
   - Title/breadcrumbs: `text-foreground`
   - Actions: subdued pill system — `rounded-full bg-muted/50 border border-border/50 px-3 py-1.5 text-sm text-muted-foreground hover:text-foreground`
   - Active state: `text-primary` (key color only)
   - Remove any hardcoded hex colors
   - Replace `text-text-*` → `text-foreground` / `text-muted-foreground`
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/dashboard/components/navbar.tsx` returns nothing
- `rg "text-text-" app/[locale]/dashboard/components/navbar.tsx` returns nothing
- `rg "rounded-full" app/[locale]/dashboard/components/navbar.tsx` returns at least one match (pill actions)
- `npm run typecheck` passes
</acceptance_criteria>

## Task 2: Update dashboard sidebar with StyleSeed nav patterns

<objective>Sidebar nav uses StyleSeed active/hover pattern with sidebar-foreground contrast</objective>

<read_first>
- components/sidebar/dashboard-sidebar.tsx
- components/ui/unified-sidebar.tsx
</read_first>

<action>
1. In `dashboard-sidebar.tsx`:
   - Nav items: `text-sidebar-foreground` for text (per AGENTS.md sidebar contrast rule)
   - Hover: `hover:bg-sidebar-accent text-sidebar-foreground`
   - Active: `bg-sidebar-primary/10 text-sidebar-primary` (key color in 10% bg only)
   - Icons: `text-sidebar-foreground/60` inactive, `text-sidebar-primary` active
   - Group labels: `text-sidebar-foreground/40 text-xs uppercase tracking-[0.05em]`
   - No `bg-primary` for large areas (only icon badges at 10%)

2. In `unified-sidebar.tsx`:
   - Apply same token migration for any `text-text-*` / `bg-surface-*` references
   - Active/hover: same pattern as dashboard-sidebar
   - Sidebar bg: `bg-sidebar` (semantic token)
   - Border: `border-sidebar-border`
   - Collapsed state: icons only, `text-sidebar-foreground/60`

3. Per AGENTS.md: use `sidebar-primary` / `sidebar-accent` for tint backgrounds, borders, and icon emphasis only. Text stays on `sidebar-foreground`.
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" components/sidebar/dashboard-sidebar.tsx components/ui/unified-sidebar.tsx` returns nothing
- `rg "text-sidebar-foreground" components/sidebar/dashboard-sidebar.tsx` returns at least one match
- `rg "bg-sidebar-primary" components/sidebar/dashboard-sidebar.tsx` returns nothing (no large primary bg areas — only `/10` or `/15` tints)
- `npm run typecheck` passes
</acceptance_criteria>

## Task 3: Update auth pages with Type A card pattern and dark theme

<objective>Auth pages use Type A two-column layout with dark card, frost border, semantic form tokens</objective>

<read_first>
- app/[locale]/(authentication)/components/user-auth-form.tsx
- app/[locale]/(authentication)/authentication/page-client.tsx
- app/[locale]/(authentication)/authentication/page-shell.tsx
- app/[locale]/(authentication)/layout.tsx
- app/[locale]/(authentication)/layout-shell.tsx
- app/[locale]/(authentication)/loading.tsx
- app/[locale]/(authentication)/client-layout.tsx
</read_first>

<action>
1. In `layout-shell.tsx` or `layout.tsx`:
   - Auth shell bg: `bg-background` (dark)
   - Two-column layout: left side = decorative/branding, right side = form card
   - Form card: `rounded-2xl bg-card shadow-card border border-[hsl(var(--border)/0.18)] p-8`
   - On mobile: single column, card only

2. In `user-auth-form.tsx`:
   - Form container: `rounded-2xl bg-card border border-[hsl(var(--border)/0.18)] p-8`
   - Title: `text-foreground text-2xl font-bold`
   - Subtitle: `text-muted-foreground`
   - Input fields: `bg-input border-border text-foreground placeholder:text-muted-foreground`
   - Submit button: `bg-primary text-primary-foreground hover:bg-primary/90`
   - OAuth buttons: `bg-muted border border-border text-foreground hover:bg-muted/80`
   - Error messages: `text-destructive`
   - Links: `text-primary hover:text-primary/80`
   - Divider: `border-border`
   - Remove any hardcoded hex colors

3. In `page-shell.tsx`:
   - Ensure it wraps content in the Type A card pattern

4. In `loading.tsx`:
   - Loading skeleton uses dark-compatible shimmer
   - `bg-muted` skeleton bars with `animate-shimmer`

5. In `page-client.tsx`:
   - Same token migration for any light-theme references
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(authentication)/components/user-auth-form.tsx app/[locale]/(authentication)/authentication/page-client.tsx` returns nothing
- `rg "rounded-2xl" app/[locale]/(authentication)/components/user-auth-form.tsx` returns at least one match
- `rg "bg-input" app/[locale]/(authentication)/components/user-auth-form.tsx` returns at least one match
- `npm run typecheck` passes
</acceptance_criteria>

## Task 4: Update import flow visual polish (auth import page)

<objective>Import page redirect and any import-related auth pages use dark tokens</objective>

<read_first>
- app/[locale]/(authentication)/import/page.tsx
</read_first>

<action>
1. In `import/page.tsx`:
   - Page bg: `bg-background`
   - Redirect notice: `text-muted-foreground`
   - Loading spinner: `border-primary` (key color only)
   - Remove any light-theme references

2. This is a redirect page per AGENTS.md ("Legacy localized import redirect"). Keep the redirect logic intact, only update visual tokens.
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(authentication)/import/page.tsx` returns nothing
- `npm run typecheck` passes
</acceptance_criteria>
