# 01-VISUAL-REFRESH — Plan 02: Homepage + High-Traffic Pages

---
phase: "01"
plan: "02"
type: "frontend"
wave: 2
depends_on:
  - "01"
files_modified:
  - "app/globals.css"
  - "tailwind.config.ts"
  - "components/animation/enhanced-motion.tsx"
  - "app/[locale]/(landing)/components/marketing-layout-shell.tsx"
  - "app/[locale]/(landing)/components/hero.tsx"
  - "app/[locale]/(landing)/components/features.tsx"
  - "app/[locale]/(landing)/components/how-it-works.tsx"
  - "app/[locale]/(landing)/components/partners.tsx"
  - "app/[locale]/(landing)/components/ai-feature.tsx"
  - "app/[locale]/(landing)/components/chat-feature.tsx"
  - "app/[locale]/(landing)/components/import-feature.tsx"
  - "app/[locale]/(landing)/components/calendar-preview.tsx"
  - "app/[locale]/(landing)/components/pnl-per-contract-preview.tsx"
  - "app/[locale]/(landing)/components/performance-visualization-chart.tsx"
  - "app/[locale]/(landing)/components/problem-statement.tsx"
  - "app/[locale]/(landing)/components/qualification.tsx"
  - "app/[locale]/(landing)/components/completed-timeline.tsx"
  - "app/[locale]/(landing)/components/faq.tsx"
  - "app/[locale]/(landing)/components/navbar.tsx"
  - "app/[locale]/(landing)/components/footer.tsx"
  - "app/[locale]/(landing)/components/rolling-ad-banner.tsx"
  - "app/[locale]/(landing)/propfirms/page.tsx"
  - "app/[locale]/(landing)/pricing/page.tsx"
  - "app/[locale]/(landing)/deals/page.tsx"
files_created: []
autonomous: false
requirements_addressed:
  - "REQ-VISUAL-001"
  - "REQ-VISUAL-002"
  - "REQ-VISUAL-003"
  - "REQ-VISUAL-004"
  - "REQ-VISUAL-005"
---

---

## <objective>

Apply StyleSeed 4-section types to the homepage (hero, features, how-it-works, partners, chat, import) and 3 high-traffic pages (propfirms, pricing, deals). Replace inline component patterns with StyleSeed pattern components. Fix hero theme inconsistency (light hero → dark hero). Apply `space-y-6` and `mx-6`/`px-6` discipline throughout.

---

## <read_first>

- `docs/superpowers/specs/2026-04-08-visual-refresh-design.md` — full design decisions
- `engine/DESIGN-LANGUAGE.md` — Rules 14, 18, 19, 61-63
- `engine/CLAUDE.md` — Component Usage Rules
- `app/[locale]/(landing)/components/hero.tsx` — current hero (to be replaced)
- `app/[locale]/(landing)/components/features.tsx` — current features grid
- `app/[locale]/(landing)/components/how-it-works.tsx` — current pipeline
- `app/[locale]/(landing)/components/partners.tsx` — current partner logos
- `app/[locale]/(landing)/components/ai-feature.tsx` — current AI feature
- `app/[locale]/(landing)/components/chat-feature.tsx` — current chat feature
- `app/[locale]/(landing)/components/import-feature.tsx` — current import feature
- `app/[locale]/(landing)/components/calendar-preview.tsx` — current calendar
- `app/[locale]/(landing)/components/pnl-per-contract-preview.tsx` — current chart preview
- `app/[locale]/(landing)/components/performance-visualization-chart.tsx` — current viz
- `app/[locale]/(landing)/components/problem-statement.tsx` — current problem
- `app/[locale]/(landing)/components/qualification.tsx` — current qualification
- `app/[locale]/(landing)/components/completed-timeline.tsx` — current timeline
- `app/[locale]/(landing)/components/faq.tsx` — current FAQ
- `app/[locale]/(landing)/components/marketing-layout-shell.tsx` — layout wrapper
- `app/[locale]/(landing)/components/navbar.tsx` — nav (fix theme consistency)
- `app/[locale]/(landing)/components/footer.tsx` — footer (update spacing)
- `app/[locale]/(landing)/components/rolling-ad-banner.tsx` — badge pattern
- `components/patterns/` — all 12 new pattern components from Plan 01

---

## <tasks>

### 7. Homepage — Hero (Type D Hero Card)

**Files**: `app/[locale]/(landing)/components/hero.tsx`  
**Action**: Replace current hero with StyleSeed Type D hero:
- Use `HeroCard` pattern from `components/patterns/hero-card.tsx`
- Structure: `rounded-2xl p-8 overflow-hidden` with transparent watermark background
- Icon badge: 32px, rounded-xl, `bg-primary/10`, icon: `size-[18px]` with `strokeWidth={2}`
- Label: 12px uppercase + `tracking-[0.05em]` — "YOUR TRADING COMMAND CENTER"
- Value: 48px bold + unit (24px) — "3.8M" — `leading-none`, 2:1 ratio, `ms-0.5`
- Trend: "+12.4% ↑" with `text-success`, secondary label "vs. last month" in `text-text-tertiary`
- **FIX**: Remove light theme (`bg-white`, `#181e25`). Use dark theme throughout. Current hero is light but shell is dark — this inconsistency must be resolved.
- Motion: blur-in entrance via `MotionStagger` with `variant="blur"`
- Watermark: transparent icon (opacity-6) positioned absolutely behind content

**Acceptance criteria**:
- [x] Hero renders with dark theme (no `bg-white`, no light hex colors)
- [x] Value+unit follows 2:1 ratio, `whitespace-nowrap`
- [x] Blur entrance animation plays on load
- [x] Transparent watermark visible at low opacity
- [x] `npm run typecheck` passes

---

### 8. Homepage — Features + Partners (Type B Grid)

**Files**: `app/[locale]/(landing)/components/features.tsx`, `partners.tsx`  
**Action**: Apply Type B section layout to features grid:
- Container: `px-6` (not mx-6 — grid fills width)
- Grid: `grid-cols-2 md:grid-cols-4 gap-4`
- Each feature card: `rounded-2xl p-6 bg-card shadow-card`
- Feature card structure: icon badge (28px) → label → optional metric
- Apply `space-y-6` above and below the section (not `gap-*` on the section itself)

Apply same Type B pattern to `partners.tsx`:
- Container: `px-6`
- Grid: `grid-cols-2 md:grid-cols-5 gap-6`
- Logos: grayscale by default, `hover:grayscale-0` transition

**Acceptance criteria**:
- [x] Features section uses `px-6` container with grid children
- [x] Each feature card is independent rounded-2xl p-6
- [x] Partners section uses `px-6` container with grid
- [x] Section gap above/below uses `space-y-6` on parent

---

### 9. Homepage — How It Works (Type A + Timeline)

**Files**: `app/[locale]/(landing)/components/how-it-works.tsx`  
**Action**: Wrap in SectionCard pattern:
- Container: `mx-6` (single floating card)
- Title: 18px bold with SectionCard title slot
- Steps: timeline visualization (existing) — add border-t divider between steps and optional stats
- Apply bottom stats grid if there are aggregate metrics (grid-cols-3)
- Use `MotionStagger` for step-by-step entrance

**Acceptance criteria**:
- [x] Section wrapped in `rounded-2xl p-6 shadow-card mx-6`
- [x] Section gap uses `space-y-6` on parent
- [x] Timeline entrance animated with stagger

---

### 10. Homepage — AI + Chat + Import + Calendar + PnL + Viz (Type A)

**Files**: `ai-feature.tsx`, `chat-feature.tsx`, `import-feature.tsx`, `calendar-preview.tsx`, `pnl-per-contract-preview.tsx`, `performance-visualization-chart.tsx`  
**Action**: Apply Type A section card pattern to each:
- Each wraps in `rounded-2xl p-6 shadow-card bg-card mx-6`
- Header: icon badge + label (12px uppercase)
- Content: the existing feature content
- If chart present: add `border-t border-surface-muted pt-5` divider + bottom stats grid
- Use `space-y-6` for section gaps between these sections

For `pnl-per-contract-preview.tsx` and `performance-visualization-chart.tsx`:
- Apply StyleSeed chart rules (Rule 17): area chart with `strokeWidth={2.5}`, gradient fill 15%→0%, no dots, hidden axes
- Chart height: `h-40` (160px) with `-mx-2` adjustment

**Acceptance criteria**:
- [x] All 6 feature sections wrapped in Type A card pattern
- [x] Chart sections have border-t divider + bottom stats grid
- [x] Area charts follow Rule 17 styling
- [x] Section gaps use `space-y-6`

---

### 11. Homepage — Problem + Qualification + Timeline (Type A)

**Files**: `problem-statement.tsx`, `qualification.tsx`, `completed-timeline.tsx`, `faq.tsx`  
**Action**: Apply Type A card pattern to each:
- `mx-6` single floating card
- Title mb-4 or mb-6 depending on content (mb-6 before charts, mb-4 before lists)
- `border-t border-surface-muted` dividers where appropriate

For `faq.tsx`:
- Use SectionCard with title
- Accordion items: each `rounded-lg bg-surface-muted p-4`
- Apply StyleSeed Rule 9 for any status indicators

**Acceptance criteria**:
- [x] All 4 sections wrapped in Type A pattern
- [x] Proper title margins (mb-4 or mb-6)
- [x] Section gaps use `space-y-6`

---

### 12. Navbar — Theme consistency + StyleSeed nav patterns

**Files**: `app/[locale]/(landing)/components/navbar.tsx`  
**Action**: Update navbar to match StyleSeed nav patterns:
- Active nav: `text-primary` with underline or background tint
- Inactive nav: `text-text-secondary`
- Nav items: `text-sm font-medium` (not font-bold)
- Hover: `hover:text-primary` transition
- CTA button: use `ButtonV2` with primary variant
- Mobile nav: full-width panel, `rounded-2xl` card style
- Ensure navbar background is dark (matches shell): `bg-background/80 backdrop-blur`
- No light theme elements in navbar

**Acceptance criteria**:
- [x] Navbar uses dark theme throughout (no light backgrounds)
- [x] Active state uses `--primary` color
- [x] Mobile nav has proper card styling
- [x] CTA button uses ButtonV2 component

---

### 13. Footer — StyleSeed spacing + typography

**Files**: `app/[locale]/(landing)/components/footer.tsx`  
**Action**: Apply StyleSeed spacing and typography to footer:
- Footer container: `bg-card rounded-t-2xl p-8` (generous padding)
- Column headers: 12px uppercase + `tracking-[0.05em]` per StyleSeed label rule
- Links: 14px body text, `text-text-secondary`, hover `text-text-primary`
- Brand/description: `text-text-tertiary` for secondary info
- Bottom bar: `border-t border-border` with `pt-6`
- Social icons: `size-5` (20px), `text-text-tertiary`, hover `text-primary`
- Use `space-y-6` between footer sections

**Acceptance criteria**:
- [x] Footer uses StyleSeed label typography (12px uppercase)
- [x] Link hover states use `text-text-primary`
- [x] Proper `space-y-6` section spacing
- [x] Social icons follow 20px size convention

---

### 14. Rolling Ad Banner — Badge pattern

**Files**: `app/[locale]/(landing)/components/rolling-ad-banner.tsx`  
**Action**: Apply StyleSeed badge pattern:
- Badge container: `bg-primary/10 rounded-full px-3 py-1`
- Badge text: 11px medium `text-primary uppercase tracking-wide`
- Scroll animation: `duration-300` transition
- If used as "NEW" or "UPDATED" indicator: use `bg-success/10 text-success`

**Acceptance criteria**:
- [x] Badge uses 10% opacity background pattern
- [x] Text is 11px uppercase
- [x] Scroll animation is smooth (no jank)

---

### 15. PropFirms page — Catalogue experience (Type A/B hybrid)

**Files**: `app/[locale]/(landing)/propfirms/page.tsx`, `PropFirmCatalogueExperience`  
**Action**: Read the current PropFirmCatalogueExperience component:
- If it uses a grid of firm cards → apply Type B (`px-6`, `grid-cols-2 md:grid-cols-3`)
- Each firm card: `rounded-2xl p-4 bg-card shadow-card`
- Search/filter bar: wrap in SectionCard Type A
- Apply `space-y-6` between catalogue sections
- If there are stat displays (account count, payout amounts) → use StatCard pattern
- PropFirm stats: number+unit 2:1 ratio, `whitespace-nowrap`

**Acceptance criteria**:
- [x] Catalogue grid uses `px-6` container with grid children
- [x] Firm cards are independent rounded-2xl cards
- [x] Stat displays use 2:1 number+unit ratio
- [x] `npm run typecheck` passes

---

### 16. Pricing page — Pricing cards (Type B)

**Files**: `app/[locale]/(landing)/pricing/page.tsx`  
**Action**: Read the current pricing page:
- If it has pricing tier cards → apply Type B grid layout
- Each tier: `rounded-2xl p-6 bg-card shadow-card`
- "Popular" or featured tier: add `border-2 border-primary` highlight
- Price display: number (36px bold) + period (18px) — 2:1 ratio
- Feature list items: check icon (12px) + text — use `text-text-secondary`
- CTA button: use ButtonV2, full-width at bottom of card
- Apply `space-y-6` between pricing sections
- FAQ or comparison section → Type A card

**Acceptance criteria**:
- [x] Pricing cards use grid layout with `px-6` container
- [x] Featured tier has primary color border
- [x] Price displays follow 2:1 ratio
- [x] `npm run typecheck` passes

---

### 17. Deals page — Deal cards (Type B or Type C)

**Files**: `app/[locale]/(landing)/deals/page.tsx`  
**Action**: Read the current deals page:
- If 2-4 deals → Type B grid (2 columns)
- If 5+ deals or promotional → Type C carousel (`overflow-x-auto`, `w-[280px]` cards)
- Each deal card: `rounded-2xl p-6 bg-card shadow-card`
- Deal amount: number (36px bold) + unit — 2:1 ratio
- Deal badge: urgency indicator using StyleSeed impact color system
- Expiry date: 13px `text-text-tertiary`, `whitespace-nowrap`
- CTA: "Claim Deal" button → ButtonV2 primary

**Acceptance criteria**:
- [x] Deal cards use Type B or Type C section pattern
- [x] Deal amounts follow 2:1 ratio
- [x] Urgency badges use correct impact color sizing (6px dot + 11px text max)
- [x] `npm run typecheck` passes

---

## <verification>

1. Run `npm run typecheck` — all modified files pass TypeScript
2. Run `npm run lint` — no new lint errors
3. Visual QA: screenshot `/en` homepage — compare against Resend/Expo reference
4. Visual QA: `/en/propfirms`, `/en/pricing`, `/en/deals`
5. Verify hero is dark (no light backgrounds, no `#181e25`)
6. Verify all sections use `space-y-6` gaps
7. Verify no inline hex colors in modified components (grep for `#[0-9a-fA-F]{6}` patterns that should use CSS vars)

---

## <success_criteria>

- [x] Homepage hero is dark theme (no light backgrounds)
- [x] All homepage sections use `mx-6` (single) or `px-6` (grid/carousel) correctly
- [x] All section gaps use `space-y-6`
- [x] All chart areas follow Rule 17 styling
- [x] All number+unit displays use 2:1 ratio with `whitespace-nowrap`
- [x] All labels use 12px uppercase + `tracking-[0.05em]`
- [x] Navbar and footer use dark theme consistently
- [x] PropFirms, Pricing, Deals pages updated with Type A/B/C patterns
- [x] No inline hex colors in modified components (use CSS variables)
- [x] `npm run typecheck` passes
- [x] `npm run lint` passes
