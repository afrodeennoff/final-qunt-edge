---
wave: 2
depends_on: ['01', '02', '03']
files_modified:
  - app/[locale]/(landing)/propfirms/page.tsx
  - app/[locale]/(landing)/pricing/page.tsx
  - app/[locale]/(landing)/deals/page.tsx
  - app/[locale]/(landing)/faq/page.tsx
  - app/[locale]/(landing)/blogs/page.tsx
  - app/[locale]/(landing)/community/page.tsx
  - app/[locale]/(landing)/leaderboard/page.tsx
  - app/[locale]/(landing)/support/page.tsx
  - app/[locale]/(landing)/docs/page.tsx
  - app/[locale]/(landing)/about/page.tsx
  - app/[locale]/(landing)/terms/page.tsx
  - app/[locale]/(landing)/privacy/page.tsx
  - app/[locale]/(landing)/newsletter/page.tsx
  - app/[locale]/(landing)/referral/page.tsx
  - app/[locale]/(landing)/disclaimers/page.tsx
  - app/[locale]/(landing)/best-trading-journal/page.tsx
  - app/[locale]/(landing)/firm/page.tsx
  - app/[locale]/(landing)/_updates/page.tsx
  - app/[locale]/(landing)/updates/page.tsx
  - app/[locale]/(landing)/maintenance/page.tsx
  - app/[locale]/(landing)/components/footer.tsx
  - app/[locale]/(landing)/components/navbar.tsx
requirements:
  - REQ-VISUAL-001
  - REQ-VISUAL-005
autonomous: true
must_haves:
  - All 20 landing pages use semantic dark tokens
  - No hardcoded hex colors
  - Cards use rounded-2xl with frost border
  - Navbar and footer use dark theme tokens
  - Section types applied (Type A/B/C/D where appropriate)
---

# Plan 05: Landing Pages — Dark Theme & Section Types

## Task 1: Update Navbar and Footer components

<objective>Marketing navbar and footer use dark-theme semantic tokens consistently</objective>

<read_first>
- app/[locale]/(landing)/components/navbar.tsx
- app/[locale]/(landing)/components/footer.tsx
</read_first>

<action>
1. In `navbar.tsx`:
   - Replace any hardcoded hex colors with semantic tokens
   - Nav links: `text-muted-foreground hover:text-foreground transition-colors`
   - Active link: `text-primary` (key color for active state only)
   - Nav bg: `bg-background/80 backdrop-blur-md` (frosted glass effect)
   - Mobile menu: `bg-card border border-[hsl(var(--border)/0.18)]`
   - Logo: `text-foreground`

2. In `footer.tsx`:
   - Footer bg: `bg-card`
   - Section titles: `text-foreground font-semibold`
   - Links: `text-muted-foreground hover:text-foreground transition-colors`
   - Divider: `border-border`
   - Social icons: `text-muted-foreground hover:text-primary`
   - Bottom text: `text-muted-foreground text-sm`
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(landing)/components/navbar.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(landing)/components/footer.tsx` returns nothing
- `rg "text-muted-foreground" app/[locale]/(landing)/components/navbar.tsx` returns at least one match
- `npm run typecheck` passes
</acceptance_criteria>

## Task 2: Update PropFirms and Pricing landing pages

<objective>Key conversion pages use Type A/B hybrid layout with dark tokens</objective>

<read_first>
- app/[locale]/(landing)/propfirms/page.tsx
- app/[locale]/(landing)/pricing/page.tsx
</read_first>

<action>
1. In `propfirms/page.tsx`:
   - Catalogue cards: `rounded-2xl bg-card shadow-card border border-[hsl(var(--border)/0.18)]`
   - Grid: `gap-4` (Type B)
   - Filter chips: `bg-muted text-muted-foreground` inactive, `bg-primary/10 text-primary` active
   - Remove any hardcoded hex colors
   - Use `text-foreground` / `text-muted-foreground` for text hierarchy

2. In `pricing/page.tsx`:
   - Pricing cards: `rounded-2xl bg-card shadow-card border border-[hsl(var(--border)/0.18)]`
   - Featured card: `border-primary` (key color border only)
   - Price: `text-foreground` with `text-[36px]` / unit `text-[18px]` (2:1 ratio)
   - Feature list checkmarks: `text-success` (semantic)
   - CTA buttons: `bg-primary text-primary-foreground`
   - Grid: `gap-4` (Type B)
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(landing)/propfirms/page.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(landing)/pricing/page.tsx` returns nothing
- `rg "rounded-2xl" app/[locale]/(landing)/propfirms/page.tsx` returns at least one match
- `rg "rounded-2xl" app/[locale]/(landing)/pricing/page.tsx` returns at least one match
- `npm run typecheck` passes
</acceptance_criteria>

## Task 3: Update Deals, FAQ, Blogs, Community pages

<objective>Content-heavy pages use consistent dark tokens and card patterns</objective>

<read_first>
- app/[locale]/(landing)/deals/page.tsx
- app/[locale]/(landing)/faq/page.tsx
- app/[locale]/(landing)/blogs/page.tsx
- app/[locale]/(landing)/community/page.tsx
</read_first>

<action>
1. `deals/page.tsx`:
   - Deal cards: `rounded-2xl bg-card shadow-card border border-[hsl(var(--border)/0.18)]`
   - Use Type B grid (`gap-4`) or Type C carousel (`overflow-x-auto scrollbar-hide`) depending on existing layout
   - Badge/discount: `bg-primary/10 text-primary` (key color badge only)

2. `faq/page.tsx`:
   - FAQ items: `rounded-2xl bg-card border border-[hsl(var(--border)/0.18)]`
   - Question text: `text-foreground font-semibold`
   - Answer text: `text-muted-foreground`
   - Use Accordion component if available, else collapsible divs

3. `blogs/page.tsx`:
   - Blog cards: `rounded-2xl bg-card shadow-card border border-[hsl(var(--border)/0.18)]`
   - Date/category: `text-muted-foreground text-sm`
   - Title: `text-foreground font-semibold`

4. `community/page.tsx`:
   - Section cards: `rounded-2xl bg-card border border-[hsl(var(--border)/0.18)]`
   - CTA buttons: `bg-primary text-primary-foreground`

For all: remove hardcoded hex colors, use semantic tokens.
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(landing)/deals/page.tsx app/[locale]/(landing)/faq/page.tsx app/[locale]/(landing)/blogs/page.tsx app/[locale]/(landing)/community/page.tsx` returns nothing
- Each file has at least one `rounded-2xl` match
- `npm run typecheck` passes
</acceptance_criteria>

## Task 4: Update remaining landing pages (leaderboard, support, docs, about, terms, privacy, newsletter, referral, disclaimers, best-trading-journal, firm, updates, maintenance)

<objective>All remaining landing pages use dark semantic tokens and frost-border card system</objective>

<read_first>
- app/[locale]/(landing)/leaderboard/page.tsx
- app/[locale]/(landing)/support/page.tsx
- app/[locale]/(landing)/docs/page.tsx
- app/[locale]/(landing)/about/page.tsx
- app/[locale]/(landing)/terms/page.tsx
- app/[locale]/(landing)/privacy/page.tsx
- app/[locale]/(landing)/newsletter/page.tsx
- app/[locale]/(landing)/referral/page.tsx
- app/[locale]/(landing)/disclaimers/page.tsx
- app/[locale]/(landing)/best-trading-journal/page.tsx
- app/[locale]/(landing)/firm/page.tsx
- app/[locale]/(landing)/_updates/page.tsx
- app/[locale]/(landing)/updates/page.tsx
- app/[locale]/(landing)/maintenance/page.tsx
</read_first>

<action>
1. For each page file, apply consistent token migration:
   - Replace any `text-text-*` with `text-foreground` / `text-muted-foreground`
   - Replace any `bg-surface-*` with `bg-card` / `bg-muted`
   - Replace any hardcoded hex with semantic tokens
   - Card wrappers: `rounded-2xl bg-card border border-[hsl(var(--border)/0.18)]`
   - Headings: `text-foreground`
   - Body text: `text-muted-foreground`

2. For `leaderboard/page.tsx`:
   - Use RankedList pattern component if appropriate
   - Highlighted user row: `bg-primary/8 border-primary/20`

3. For `firm/page.tsx`:
   - Firm detail card: Type A (`mx-6 rounded-2xl p-6`)
   - Stats: use StatCard pattern with 2:1 ratio

4. For `terms/page.tsx`, `privacy/page.tsx`, `disclaimers/page.tsx`:
   - Prose content: ensure dark-compatible `prose-invert` or custom prose styling
   - Section headings: `text-foreground`

5. For `newsletter/page.tsx`, `referral/page.tsx`:
   - CTA sections: `bg-primary/5` subtle tint (not solid primary bg)
   - Input fields: `bg-input border-border text-foreground`

6. For `maintenance/page.tsx`:
   - Centered message: `text-foreground`
   - Icon: `text-muted-foreground`
</action>

<acceptance_criteria>
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(landing)/leaderboard/page.tsx app/[locale]/(landing)/support/page.tsx app/[locale]/(landing)/docs/page.tsx app/[locale]/(landing)/about/page.tsx app/[locale]/(landing)/terms/page.tsx app/[locale]/(landing)/privacy/page.tsx` returns nothing
- `rg "#[0-9a-fA-F]{3,6}" app/[locale]/(landing)/newsletter/page.tsx app/[locale]/(landing)/referral/page.tsx app/[locale]/(landing)/disclaimers/page.tsx app/[locale]/(landing)/best-trading-journal/page.tsx` returns nothing
- `npm run typecheck` passes
</acceptance_criteria>
