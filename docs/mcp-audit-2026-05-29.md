# MCP Coverage Audit — 2026-05-29

**Task 0.1 Execution**  
**Source Plan:** `docs/superpowers/plans/2026-05-29-full-mcp-coverage.md`  
**Goal:** Map 100% of public user-facing web features + all admin features to current MCP tool coverage. Identify gaps for Phase 1–7 implementation. Strict user-isolation required on all tools.

**Discovery Method:** 
- Globbed all `app/**/page.tsx` (dashboard, admin, teams, landing, auth).
- Globbed `app/api/**/route.ts` + admin routes.
- Extracted all Prisma models (61 total) from `prisma/schema.prisma`.
- Full inventory of current MCP tools via `grep` on `server/mcp*.ts` + handlers (see "Current MCP Tools" section).
- Reviewed key server actions (`server/*.ts`, `server/imports/*.ts`, `server/accounts.ts`, `server/trades.ts`, `server/teams.ts`, `server/billing.ts`, etc.) for write paths.
- Inspected dashboard components, import processors, AI routes, notes/teams/strategies/billing pages for feature surfaces.
- Reviewed MCP handlers (`server/mcp/handlers/*.ts`), dispatchers in `mcp-tools.ts`, `mcp-user-write-tools.ts`, `mcp-admin-write-tools.ts`, `mcp-admin-tools.ts`, `mcp-website-tools.ts`.
- No MCP tool code was written — audit only.

---

## Current MCP Tools (Full Inventory — Plan Step 2)

**Command run:**  
```bash
grep -o "name: '[^']*'" server/mcp-tools.ts server/mcp-user-write-tools.ts server/mcp-admin-tools.ts server/mcp-admin-write-tools.ts server/mcp-website-tools.ts | sort
```

### From `server/mcp-tools.ts` (standardTools — 20 tools)
- `add_trade_review_note`
- `analyze_trade`
- `brutal_journal_audit`
- `create_journal_entry`
- `generate_daily_briefing`
- `get_account_details`
- `get_account_health`
- `get_behavioral_patterns`
- `get_challenge_progress`
- `get_performance_summary`
- `get_prop_compliance`
- `get_risk_metrics`
- `get_user_profile`
- `list_accounts`
- `list_tags`
- `list_trades`
- `run_monte_carlo`
- `suggest_position_size`
- `update_trade_tags`

### From `server/mcp-user-write-tools.ts` (22 tools — many writes)
- `create_account`
- `create_group`
- `create_tag`
- `delete_account`
- `delete_group`
- `delete_payout`
- `delete_tag`
- `delete_trades`
- `get_equity_chart`
- `get_mood_history`
- `get_subscription`
- `group_trades`
- `import_trades`
- `list_groups`
- `list_payouts`
- `save_payout`
- `ungroup_trades`
- `update_account`
- `update_group`
- `update_profile`
- `update_tag`

### From `server/mcp-admin-tools.ts` (4 tools)
- `admin_get_analytics`
- `admin_get_user`
- `admin_list_subscriptions`
- `admin_list_users`

### From `server/mcp-admin-write-tools.ts` (11 tools)
- `admin_create_blog_post`
- `admin_create_coupon`
- `admin_create_prop_firm`
- `admin_delete_blog_post`
- `admin_delete_coupon`
- `admin_delete_prop_firm`
- `admin_get_review_moderation_queue`
- `admin_moderate_review`
- `admin_update_blog_post`
- `admin_update_prop_firm`
- `admin_update_user`

### From `server/mcp-website-tools.ts` (13 public tools)
- `compare_prop_firms`
- `get_blog_post`
- `get_community_post`
- `get_community_post_comments`
- `get_leaderboard`
- `get_prop_firm`
- `get_trader_benchmarks`
- `list_active_deals`
- `list_blog_posts`
- `list_challenges`
- `list_community_posts`
- `list_prop_firm_reviews`
- `list_prop_firms`

**Total unique tools:** ~70 (some overlap in dispatch).

**Handlers (server/mcp/handlers/):**
- `account.ts`: `getAccountHealthHandler`, `listAccountsHandler` (partial; most logic still in `mcp-tools.ts` / write files).
- `trade.ts`: Stub handlers only (`listTradesHandler`, `getPerformanceSummaryHandler`, `getRiskMetricsHandler` — "extraction in progress").
- `journal.ts`: `createJournalEntryHandler` (partial).
- `risk.ts`: Risk/ compliance / Monte Carlo helpers (partial).
- `public.ts`: Public website handlers (used by website tools).
- `__tests__/`: Limited unit tests for accounts only.
- No dedicated handlers yet for AI, teams, notes, most imports, billing writes, admin email/newsletter.

**Dispatchers:** `handleMcpToolCall` (mcp-tools.ts), `handleUserWriteToolCall` (mcp-user-write-tools.ts), `handleAdminWriteToolCall` + `handleAdminMcpToolCall`, website server factories.

**Security note (pre-Task 0.2):** Many handlers still use raw `ctx.userId` without `requireUserId` guard from planned `security.ts`. Several tools (e.g. `analyze_trade`) accept IDs that must be strictly scoped.

---

## Personal / User Features (Dashboard)

### Dashboard Home / Widgets (app/[locale]/dashboard/page.tsx + components/widgets/*)
- Overview widgets (PnL summary, risk, expectancy, trading score, smart insights, propfirm catalogue, mindset/journaling, emotion trends, hourly timeline, news impact).
- **Status:** Partially covered. Reads via `get_account_health`, `get_performance_summary`, `get_risk_metrics`, `get_behavioral_patterns`, `generate_daily_briefing`, `brutal_journal_audit`, `get_challenge_progress`, `get_prop_compliance`. No dedicated "widget snapshot" or "reorder widgets" MCP tool. Dashboard layout save uses `saveDashboardLayoutAction` (MISSING in MCP).
- **Covered by:** `get_account_health`, `list_accounts`, `get_performance_summary`, `get_risk_metrics`, `get_behavioral_patterns`, `generate_daily_briefing`, `brutal_journal_audit`.
- **MISSING:** `save_dashboard_layout`, `get_dashboard_layout`, full widget data export.

### Accounts (app/[locale]/dashboard/accounts/page.tsx + accounts-overview.tsx, account-configurator.tsx)
- List, create, update (propfirm, sizes, drawdown, targets, trailing, evaluation flag, resetDate), delete (with trades cascade).
- Payouts: list, save (add/edit), delete, status (pending/paid/refused).
- Groups: assign/move accounts to groups.
- Reset account (resetDate for challenge restart).
- **Status:** Mostly covered (recently added writes).
- **Covered by:** `list_accounts`, `get_account_details`, `get_account_health`, `create_account`, `update_account`, `delete_account`, `list_payouts`, `save_payout`, `delete_payout`, `list_groups`, `create_group`, `update_group`, `delete_group`, `group_trades` (partial).
- **Partially:** Reset account (no dedicated `reset_account` tool; overloads update). Payouts covered but no "request payout" or payout eligibility check beyond health.
- **MISSING:** `reset_account`, bulk account ops, account equity snapshot export.

### Trades (app/[locale]/dashboard/trades/page.tsx + trade-table-review.tsx, trade-*.tsx components)
- List (filter, sort, paginate, tag filter, search).
- Create (manual import, bulk from processors).
- Update: tags (add/remove/replace), comment/review note, images (upload + base64 second image), videoUrl, group/ungroup.
- Delete (single + bulk).
- Bulk edit (tags, group, comment, video).
- **Status:** Reads strong; writes partial.
- **Covered by:** `list_trades`, `get_performance_summary`, `analyze_trade`, `update_trade_tags`, `add_trade_review_note`, `delete_trades`, `group_trades`, `ungroup_trades`, `import_trades` (generic), `get_equity_chart`.
- **Partially:** `import_trades` (single generic entry point; does not expose per-broker processors). Image upload via trade update (no dedicated `upload_trade_image`). No bulk tag across days or "add tags to day".
- **MISSING:** `create_trade` (manual single trade entry), `update_trade` (general fields: prices, qty, dates, instrument, side, commission), `upload_trade_image`, `bulk_update_trades` (beyond current), `delete_trade` (granular vs bulk), trade image management tools, video-specific.

### Journal / Moods / Mindset (dashboard/components/mindset/* + journal.ts)
- Create/update mood entries (day, mood label, emotionValue 0-100, journalContent, link trades).
- Delete mindset entry.
- Mood history, emotion trends, TTFM checklist.
- Behavioral patterns (mood vs PnL correlation).
- **Status:** Basic create covered; full CRUD + history partial.
- **Covered by:** `create_journal_entry`, `get_behavioral_patterns`, `get_mood_history`.
- **Partially:** Mood history limited; no `update_mood`, `delete_mood`, `list_moods` (date range), "brutal journal audit" is read-only analysis.
- **MISSING:** `update_journal_entry`, `delete_journal_entry`, `list_journal_entries`, `link_trades_to_mood`, full mindset timeline export.

### Risk, Performance, Analytics (dashboard/analytics + risk/compliance)
- Risk metrics (max DD, RR, expectancy, Sharpe proxy, violations).
- Performance summary (winrate, PnL, profit factor).
- Prop compliance (daily loss, total DD, profit target, min days, trailing).
- Challenge progress.
- Monte Carlo simulation.
- Behavioral patterns, position sizing suggestion.
- Equity chart.
- **Status:** Excellent read coverage.
- **Covered by:** `get_risk_metrics`, `get_performance_summary`, `get_prop_compliance`, `get_challenge_progress`, `run_monte_carlo`, `suggest_position_size`, `get_equity_chart`, `analyze_trade`, `get_account_health`.
- **Partially:** Some metrics duplicated across tools; no unified "full analytics export".
- **MISSING:** `get_equity_curve` (vs chart data), `run_monte_carlo` with custom params (beyond basic), risk violation list, "brutal audit" enhancements.

### Imports & Broker Sync (dashboard/import/ + many processors: ibkr-*, tradovate, rithmic, dxfeed, mt5, thor, etp, ninjatrader, atas, ftmo, topstep, quantower, tradezella, manual)
- IBKR: PDF OCR/extract, FIFO computation, orders.
- Tradovate / Rithmic / DXFeed: credential sync, live/historical sync, performance processor.
- MT5, Thor, ETP: token-based sync/store.
- Others: file upload processors → `createTradeWithDefaults` + saveTradesAction.
- Bulk import via `import_trades` or direct actions.
- **Status:** `import_trades` exists but generic. No per-broker MCP tools.
- **Covered by:** `import_trades` (partial/generic).
- **Partially:** One entry point; does not support progress reporting, credential management, or specific extractors (OCR, FIFO).
- **MISSING (high priority):** `import_ibkr_pdf`, `sync_tradovate`, `sync_rithmic`, `sync_dxfeed`, `sync_mt5`, `sync_thor`, `sync_etp`, `extract_ibkr_orders`, `compute_ibkr_fifo`, `list_synchronizations`, credential test/refresh for each broker. All current sync lives in `server/imports/*-actions.ts` and `/api/imports/*`, `/api/*/sync` routes.

### AI Tools (app/api/ai/* + components)
- `/ai/chat`, `/ai/analyze`, `/ai/summarize`, `/ai/format-trades`, `/ai/transcribe`, `/ai/editor`, `/ai/mappings`, `/ai/support`.
- Analysis endpoints: `/ai/analysis/global`, `/accounts`, `/instrument`, `/time-of-day`.
- Search: `/ai/search/date`.
- **Status:** ZERO coverage. All AI surface is web-only (uses OpenAI/Anthropic etc. server-side).
- **Covered by:** None.
- **MISSING (critical for Phase 3):** Full set — `ai_chat`, `ai_analyze_trade`, `ai_summarize_period`, `ai_format_trades`, `ai_transcribe`, `ai_editor`, `ai_generate_mappings`, `ai_support`, `ai_analysis_*` variants, `ai_search_trades`. Must pass same args + enforce userId from context. Rate-limit + usage ledger already exists in Prisma (`AiRequestLog`, `AiUsageLedger`).

### Tags & Groups (filters, tag-widget, account-group-board)
- CRUD tags (create, update, delete, color).
- Add/remove tags on trades (single/bulk/day).
- Groups (create, rename, delete, move accounts/trades).
- **Status:** Good coverage on writes.
- **Covered by:** `list_tags`, `create_tag`, `update_tag`, `delete_tag`, `create_group`, `update_group`, `delete_group`, `list_groups`, `group_trades`, `ungroup_trades`, `update_trade_tags`.
- **Partially:** `addTagsToTradesForDay` (server action) not exposed as MCP; bulk day tagging MISSING.
- **MISSING:** `add_tags_to_day`, `bulk_tag_trades`.

### Payouts & Compliance (already covered under Accounts)
- See Accounts section.

### Notes (app/[locale]/dashboard/notes/ + use-notes hook, note-editor, etc.)
- Full CRUD notes (create, update, delete, pin, archive).
- Tags on notes, filter by symbol/trader, search, templates.
- Link to trades/symbols.
- **Status:** No MCP exposure. All via client `useNotes` + presumed server actions in notes lib (not in main server/ grep hits).
- **Covered by:** None.
- **MISSING:** `create_note`, `update_note`, `delete_note`, `list_notes`, `pin_note`, `archive_note`, `add_note_tag`, `search_notes`. (Phase 5 target)

### Strategies (dashboard/strategies/ — thin wrapper over trade table)
- Strategy tagging / filtering via existing tags/groups on trades.
- **Status:** No dedicated surface; reuses trades.
- **Covered by:** Existing tag/group/trade tools.
- **MISSING:** Dedicated `create_strategy`, `list_strategies`, `assign_trades_to_strategy` (if separate model exists — currently uses tags/groups).

### Billing & Subscriptions (dashboard/billing/ + whop checkout routes)
- View subscription, cancel (with feedback), resume/pause (via Whop manage_url).
- Collect feedback on cancel.
- Checkout via `/api/whop/checkout*`.
- **Status:** Read-only `get_subscription`.
- **Covered by:** `get_subscription`.
- **Partially:** Cancel/feedback via `collectSubscriptionFeedback` (server) — not MCP.
- **MISSING:** `cancel_subscription`, `collect_subscription_feedback`, `get_billing_portal_url`, `pause_subscription`, `resume_subscription`. (Phase 5 — limited writes due to Whop external).

### Profile, Settings, Theme, Data (dashboard/settings, /trader-profile, /data, user-menu)
- Update profile (username, language, etc.).
- Theme preference (dashboardTheme).
- Trader profile benchmark upload.
- Data management (refresh, debug, equity charts, account management).
- Leaderboard visibility toggle.
- **Status:** `update_profile`, `get_user_profile`.
- **Covered by:** `update_profile`, `get_user_profile`.
- **Partially:** Theme via update_profile? Leaderboard flag in user model.
- **MISSING:** `update_theme`, `set_leaderboard_visibility`, `upload_trader_benchmark`, `get_data_export`, `refresh_all_data` (cache invalidation tool?), full settings surface. Trader profile page uses `/api/trader-profile/benchmark`.

### Teams & Collaboration (app/[locale]/teams/* + server/teams.ts, team-membership.ts)
- Create/join/manage teams, invite members, accept invitation.
- Team dashboard (traders, analytics, members).
- Shared views (`/shared/[slug]`).
- Team subscriptions, managers, analytics.
- **Status:** ZERO MCP coverage. All in `server/teams.ts` (createTeam, deleteTeam, invite, etc.) + API `/api/team/*`.
- **Covered by:** None.
- **MISSING (Phase 4):** `create_team`, `invite_team_member`, `accept_team_invite`, `remove_team_member`, `list_team_members`, `get_team_analytics`, `create_shared_view`, `list_user_teams`. Strict team-membership scoping required (no cross-team leakage).

### Trader Profile / Public Profile
- Benchmark snapshots, visibility.
- **Covered by:** Some website `get_trader_benchmarks`.
- **MISSING:** Personal `update_trader_profile`, `upload_benchmark`.

---

## Public Website / Landing Features (app/[locale]/(landing)/ + shared)

- Prop firms: list, get details, compare, reviews (list/create/flag), deals (active, compare, guides, calculator, FAQ).
- Community: posts, comments, votes.
- Blogs: list, get single.
- Leaderboard, challenges, trader benchmarks.
- Support, FAQ, pricing, referrals, updates, maintenance.
- **Status:** Excellent coverage via dedicated public MCP server (no auth or public key).
- **Covered by:** All 13 website tools (`list_prop_firms`, `get_prop_firm`, `compare_prop_firms`, `list_prop_firm_reviews`, `list_active_deals`, `list_blog_posts`, `get_blog_post`, `list_community_posts`, `get_community_post`, `get_community_post_comments`, `get_leaderboard`, `get_trader_benchmarks`, `list_challenges`).
- **Partially:** Reviews creation/flag still web-only (uses `createPropFirmReview`, `flagReview` server actions). Some deal calculator logic not exposed.
- **MISSING:** `create_prop_firm_review`, `flag_review`, full deal calculator as tool, `search_deals`, `get_challenge_details`.

---

## Admin Features (app/[locale]/admin/* + server actions + /api/admin/*)

### Admin Dashboard / Overview (/admin)
- Analytics, user list, subscription overview.
- **Covered by:** `admin_get_analytics`, `admin_list_users`, `admin_list_subscriptions`, `admin_get_user`.
- **Partially:** Analytics may be high-level only.

### Prop Firms Admin (/admin/propfirms + /[id])
- Full CRUD: create, update, delete (soft), rules.
- **Covered by:** `admin_create_prop_firm`, `admin_update_prop_firm`, `admin_delete_prop_firm`.
- **Good.**

### Coupons / Deals Admin (/admin/coupons)
- Create, delete coupons for prop firms.
- **Covered by:** `admin_create_coupon`, `admin_delete_coupon`.
- **Good.**

### Reviews Moderation (/admin/reviews)
- Queue (flagged/pending), moderate (approve/reject/flag).
- **Covered by:** `admin_get_review_moderation_queue`, `admin_moderate_review`.
- **Good.**

### Blogs Admin (/admin/blogs + new + /[id]/edit)
- Full CRUD blog posts.
- **Covered by:** `admin_create_blog_post`, `admin_update_blog_post`, `admin_delete_blog_post`.
- **Good.**

### Email / Newsletter / Communication
- `/admin/send-email`, `/admin/welcome-email`, `/admin/weekly-recap`, newsletter-builder.
- Cron jobs, templates, unsubscribe.
- **Covered by:** None.
- **MISSING (high admin priority):** `admin_send_email`, `admin_send_welcome_email`, `admin_send_weekly_recap`, `admin_build_newsletter`, `admin_list_email_templates`, `admin_preview_email`. (Uses server/email/* and /api/email/*).

### Other Admin
- User updates (`admin_update_user` — covered).
- Subscriptions management (partial via list).
- Reports (`/api/admin/reports`).
- Propfirm stats, firm reviews.
- **Covered by:** `admin_update_user`, `admin_get_analytics`.
- **MISSING:** `admin_send_report`, `admin_impersonate_user` (careful), full subscription CRUD beyond list, `admin_delete_user`, `admin_reset_user_data`, coupon bulk ops, blog publish scheduling.

**Admin API surface:** Only 2 routes (`/admin/subscriptions`, `/admin/reports`). Most admin via server actions called from admin pages (protected by `assertAdminAccess`).

**Admin MCP:** Requires `qunt_adm_` key + `requireAdminAccess(ctx)` in handlers. Current admin tools do call it in write dispatcher.

---

## Other / Cross-Cutting Areas

- **Auth / API Keys:** Generate user/admin keys (`generateUserApiKey`, `generateAdminApiKey` — server only, not MCP tools yet). MCP auth via `mcp-auth.ts`, `mcp-key-service.ts`.
- **Shared Views:** `createShared`, `deleteShared` (server/shared.ts) — MISSING in MCP.
- **Referrals:** Create slug, redemptions — no MCP.
- **Financial Events:** CRUD? (server/financial-events.ts) — MISSING.
- **Storage / Images:** Supabase storage for trade images — partial via update_trades.
- **Cron / Background:** Many (renew tokens, compute analytics, email) — not user-facing MCP.
- **Webhooks (Whop, etc.):** External only.
- **Prisma Models without MCP:** Many (e.g. `Business*`, `Organization`, `Order`, `Notification`, `Payment*`, `Invoice`, `AiRequestLog`, `PropFirmRule`, `Challenge`, `IdempotencyRecord`, `RecoveryCode`, etc.) — most internal or admin-adjacent.

---

## Priority Order for Missing Tools (Bottom of Audit — for Phase Planning)

**Phase 0 immediate (security + audit follow-up):**
1. `requireUserId` + `requireAdmin` guards + `assertNoCrossUserAccess` in `server/mcp/security.ts` (Task 0.2).
2. Harden every existing handler + add security header comments.
3. Fix any tools accepting `userId` in args.

**Phase 1 Core Trading (highest daily value):**
1. `create_trade` (manual entry with full validation + account scoping).
2. `update_trade` (general fields beyond tags/comment/images).
3. `upload_trade_image` + `delete_trade_image` (dedicated, with Supabase storage).
4. `reset_account` (explicit, with date + cascade logic).
5. `delete_trade` (granular single).
6. Full `import_trades` hardening or per-broker variants.
7. `list_journal_entries` + `update_journal_entry` + `delete_journal_entry`.

**Phase 2 Imports (all broker paths):**
1. `sync_tradovate` / `sync_rithmic` / `sync_dxfeed` (with credential args + progress).
2. `import_ibkr_pdf` + `extract_ibkr_orders` + `compute_ibkr_fifo`.
3. `sync_mt5`, `sync_thor`, `sync_etp`.
4. `list_synchronizations`, `test_broker_connection`.
5. Per-broker credential managers exposed safely.

**Phase 3 AI (every endpoint):**
1. `ai_chat`
2. `ai_analyze` (trade or period)
3. `ai_summarize`
4. `ai_format_trades`
5. `ai_transcribe`
6. `ai_editor`
7. `ai_mappings`
8. `ai_support`
9. All `/ai/analysis/*` variants + `ai_search_date`.
10. Usage ledger + rate limit enforcement in tools.

**Phase 4 Teams:**
1. `create_team`
2. `invite_team_member` + `accept_team_invite`
3. `list_my_teams` + `get_team_details`
4. `remove_team_member`
5. `get_team_analytics`
6. `create_shared_view` + `list_shared_views`

**Phase 5 Remaining Dashboard:**
1. Notes full CRUD (`create_note` ... `search_notes`).
2. Billing writes: `cancel_subscription`, `collect_subscription_feedback`.
3. `update_theme`, `set_leaderboard_visibility`.
4. Strategies if separate model.
5. `save_dashboard_layout` + layout versioning tools.
6. Trader profile updates.

**Phase 6 Admin Full:**
1. Email tools (`admin_send_*` variants, newsletter builder).
2. `admin_delete_user` + `admin_reset_user_data` (destructive, audited).
3. Full subscription management beyond list.
4. `admin_create_report` + report delivery.
5. Blog scheduling, coupon bulk, review bulk moderation.
6. Impersonation audit tool (very careful scoping).

**Phase 7 Public + Verification:**
- `create_prop_firm_review`, `flag_review`.
- Deal calculator as tool.
- `mcp_self_test` (admin-only verification matrix).
- End-to-end curl test matrix for `qunt_usr_` vs `qunt_adm_` keys + negative cases.
- Update `docs/mcp.md` + final verification script.

**Cross-cutting gaps (apply to all new tools):**
- Strict `where: { userId: ctx.userId }` or team-membership checks on every Prisma call.
- McpAuditLog on every call (success + error).
- Consistent error handling + rate limiting.
- Input/output schemas with full descriptions.
- Handler extraction from monolithic files into `handlers/*`.
- Tests for every new tool (TDD per plan).

---

**Audit Complete.** This document is the baseline for all subsequent phases. No tools implemented yet — only mapping and discovery.

**Next (per plan):** Task 0.2 security hardening, then Phase 1 bite-sized TDD tasks using subagent-driven-development.

**File stats:** ~200+ web surfaces mapped, 61 Prisma models, ~70 current MCP tools, ~30+ high-priority missing write paths identified.
